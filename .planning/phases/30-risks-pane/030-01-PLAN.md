---
phase: 30-risks-pane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/insights/risks/RisksAlertCard.kt
  - ui/insights/risks/RisksAlertList.kt
  - ui/insights/risks/RisksPaneHeader.kt
  - ui/insights/risks/RisksPaneEmptyState.kt
  - ui/insights/risks/RisksHistoryDisclaimer.kt
autonomous: true
requirements: [RSK-01, RSK-02, RSK-03, RSK-04, RSK-05, RSK-06, RSK-07, RSK-08]
user_setup: []
must_haves:
  truths:
    - RisksAlertCard displays icon, title (semibold 22sp), and explanation (regular 16sp) with triggering value
    - RisksAlertCard background tinted: errorContainer for WARNING severity, tertiaryContainer for INFO severity
    - RisksAlertList renders alerts vertically with 16dp spacing, WARNING alerts before INFO
    - RisksPaneHeader displays "Risks" heading (headlineMedium)
    - RisksPaneEmptyState displays "No financial activity recorded yet" when no transactions
    - RisksHistoryDisclaimer shows "Insights improve after your first full month" when !hasEnoughHistory
  artifacts:
    - path: "ui/insights/risks/RisksAlertCard.kt"
      provides: "Single alert card with severity tint, icon, title, explanation"
      contains: "fun RisksAlertCard"
    - path: "ui/insights/risks/RisksAlertList.kt"
      provides: "Vertical list of alert cards sorted by severity"
      contains: "fun RisksAlertList"
    - path: "ui/insights/risks/RisksPaneHeader.kt"
      provides: "Pane header with Risks title"
      contains: "fun RisksPaneHeader"
    - path: "ui/insights/risks/RisksPaneEmptyState.kt"
      provides: "Empty state view for no transactions"
      contains: "fun RisksPaneEmptyState"
    - path: "ui/insights/risks/RisksHistoryDisclaimer.kt"
      provides: "History disclaimer text below alert list"
      contains: "fun RisksHistoryDisclaimer"
  key_links:
    - from: "RisksAlertCard.kt"
      to: "RiskAlert"
      via: "accepts alert: RiskAlert parameter"
    - from: "RisksAlertList.kt"
      to: "RisksAlertCard"
      via: "renders RisksAlertCard for each alert in list"
    - from: "RisksAlertList.kt"
      to: "RisksUiState"
      via: "accepts alerts: List<RiskAlert> parameter"
---

<objective>
Create stateless Jetpack Compose composables for the Risks Pane UI components per 030-UI-SPEC.md.
Purpose: Implement the visual building blocks for displaying financial alerts, following Material 3 design guidelines and Phase 29 patterns.
Output: 5 stateless composable files ready for integration into the root RisksPaneScreen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-risks-pane/030-UI-SPEC.md
@.planning/REQUIREMENTS.md

<interfaces>
From InsightsUiState.kt:
```kotlin
data class RiskAlert(
    val type: String,           // e.g., "overspending", "negative_position", "high_borrowing", "expense_increase", "savings_improvement"
    val severity: String,        // "WARNING" or "INFO"
    val icon: String,            // Emoji icon
    val title: String,           // Short title
    val explanation: String,     // Detailed explanation with triggering value
    val triggeringValue: Double = 0.0
)

data class RisksUiState(
    val alerts: List<RiskAlert> = emptyList(),
    val hasTransactions: Boolean = false
)
```

From InsightsCalculator.kt (alert examples):
- negative_position: "⚠️", "Negative Net Position", WARNING
- overspending: "🚨", "Overspending Detected", WARNING
- high_borrowing: "💳", "High Borrowing", WARNING
- expense_increase: "📈", "Expense Increase", INFO
- savings_improvement: "💰", "Savings Improved", INFO
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create RisksAlertCard, RisksAlertList, RisksPaneHeader composables</name>
  <files>ui/insights/risks/RisksAlertCard.kt, ui/insights/risks/RisksAlertList.kt, ui/insights/risks/RisksPaneHeader.kt</files>
  <action>Create 3 stateless Jetpack Compose composables per 030-UI-SPEC.md:

  1. **RisksPaneHeader**: No parameters. Renders "Risks" text using Material 3 headlineMedium typography (28sp, Semibold 600 weight per UI-SPEC). Text color = MaterialTheme.colorScheme.primary.

  2. **RisksAlertCard**: Accepts `alert: RiskAlert` parameter. Renders:
     - Background: `if (alert.severity == "WARNING") MaterialTheme.colorScheme.errorContainer else MaterialTheme.colorScheme.tertiaryContainer` (per UI-SPEC severity tinting rules)
     - Row layout: Icon on left (24dp size, text color = onErrorContainer/onTertiaryContainer), text column on right
     - Title: alert.title using titleLarge typography (22sp, Semibold 600), text color = onErrorContainer/onTertiaryContainer
     - Explanation: alert.explanation using bodyLarge typography (16sp, Regular 400), text color = onErrorContainer/onTertiaryContainer at 100% opacity
     - Card elevation: 1dp (default Card elevation)
     - Internal padding: 16dp (md token)
     - Icon-to-text gap: 12dp (custom between xs=4dp and sm=8dp)

  3. **RisksAlertList**: Accepts `alerts: List&lt;RiskAlert&gt;` parameter. Renders:
     - Column with vertical scroll (use `Column(modifier = Modifier.verticalScroll(rememberScrollState()))`, NOT LazyColumn per UI-SPEC to avoid scroll interop conflicts in HorizontalPager)
     - Each RisksAlertCard with 16dp spacing between cards (lg token)
     - Alerts are already sorted by caller (WARNING before INFO per RSK-01), just render in order
     - Maximum 3 alerts (caller enforces, but composable can handle any count)

  Use Material 3 components (Card, Text, Row, Column, Icon/Text for emoji icons) only. No internal state, all data via parameters. Follow Phase 29 composable patterns (see StatusPaneHero.kt for reference).
  </action>
  <verify>
    <automated>ls ui/insights/risks/RisksAlertCard.kt ui/insights/risks/RisksAlertList.kt ui/insights/risks/RisksPaneHeader.kt 2>&1 | wc -l</automated>
  </verify>
  <done>All 3 composable files exist, each has a fun Composable with correct params, follows UI-SPEC typography/spacing/color rules, RisksAlertCard applies severity-based tinting.</done>
</task>

<task type="auto">
  <name>task 2: create RisksPaneEmptyState and RisksHistoryDisclaimer composables</name>
  <files>ui/insights/risks/RisksPaneEmptyState.kt, ui/insights/risks/RisksHistoryDisclaimer.kt</files>
  <action>Create 2 stateless Jetpack Compose composables per 030-UI-SPEC.md:

  1. **RisksPaneEmptyState**: No parameters. Renders full-screen centered Column with:
     - Text: "No financial activity recorded yet" (exact copy from RSK-08)
     - Typography: bodyLarge (16sp, Regular 400)
     - Text color: MaterialTheme.colorScheme.onSurfaceVariant
     - Fill entire screen (fillMaxSize modifier)
     - Centered vertically and horizontally (wrap content in Box with Alignment.Center)

  2. **RisksHistoryDisclaimer**: No parameters. Renders:
     - Text: "Insights improve after your first full month" (per UI-SPEC copywriting contract)
     - Typography: bodyLarge (16sp, Regular 400)
     - Text color: MaterialTheme.colorScheme.onSurfaceVariant at 60% opacity
     - Centered below alert list with 16dp top padding
     - Note: Visibility is controlled by caller based on `!hasEnoughHistory && uiState.hasTransactions`

  Use Material 3 components (Text, Column, Box) only. No internal state. Follow Phase 29 empty state patterns (see StatusPaneEmptyState.kt for reference).
  </action>
  <verify>
    <automated>ls ui/insights/risks/RisksPaneEmptyState.kt ui/insights/risks/RisksHistoryDisclaimer.kt 2>&1 | wc -l</automated>
  </verify>
  <done>Both composable files exist, RisksPaneEmptyState shows exact RSK-08 copy "No financial activity recorded yet", RisksHistoryDisclaimer shows correct disclaimer text with 60% opacity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local UI | All data from local ViewModel, no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-30-01 | I | All Composables | accept | Only render user's own financial data from local Room database |
| T-30-02 | D | All Composables | accept | No external dependencies, static UI components |
| T-30-03 | E | RiskAlert explanations | mitigate | Explanation text comes from InsightsCalculator (trusted source), displayed via Text composable (no HTML/web risks) |
</threat_model>

<verification>
1. All composables compile without errors
2. UI-SPEC typography: headlineMedium for header (28sp), titleLarge for alert titles (22sp), bodyLarge for explanations (16sp)
3. Severity tinting: WARNING alerts use errorContainer, INFO alerts use tertiaryContainer
4. Empty state copy matches RSK-08 exactly: "No financial activity recorded yet"
5. Icon size 24dp, icon-to-text gap 12dp, card internal padding 16dp (md token)
6. Column with verticalScroll (not LazyColumn) per UI-SPEC scroll rules
</verification>

<success_criteria>
5 stateless composable files created (RisksPaneHeader, RisksAlertCard, RisksAlertList, RisksPaneEmptyState, RisksHistoryDisclaimer), matching UI-SPEC.md requirements, no internal state, all data via parameters, severity-based color tinting implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/30-risks-pane/030-01-SUMMARY.md`
</output>
