---
phase: 30-risks-pane
plan: 02
type: execute
wave: 2
depends_on: ["030-01"]
files_modified:
  - ui/insights/risks/RisksPaneErrorState.kt
  - ui/insights/risks/RisksPaneScreen.kt
  - ui/screens/InsightsScreen.kt
autonomous: true
requirements: [RSK-01, RSK-02, RSK-03, RSK-04, RSK-05, RSK-06, RSK-07, RSK-08]
user_setup: []
must_haves:
  truths:
    - RisksPaneScreen displays RisksPaneHeader, RisksAlertList, RisksHistoryDisclaimer in scrollable Column
    - RisksPaneScreen shows RisksPaneEmptyState when !hasTransactions
    - RisksPaneScreen shows RisksPaneErrorState on error (separate from empty)
    - InsightsScreen.kt no longer shows "Risks Pane — coming in Phase 30" stub
    - Risks alert ordering maintained (WARNING before INFO, max 3 alerts)
    - Currency symbol displayed with all monetary values in explanations
  artifacts:
    - path: "ui/insights/risks/RisksPaneErrorState.kt"
      provides: "Error state view with retry button"
      contains: "fun RisksPaneErrorState"
    - path: "ui/insights/risks/RisksPaneScreen.kt"
      provides: "Root screen wiring ViewModel state to composables"
      contains: "fun RisksPaneScreen"
    - path: "ui/screens/InsightsScreen.kt"
      provides: "Updated to use RisksPaneScreen instead of stub"
      contains: "RisksPaneScreen("
  key_links:
    - from: "RisksPaneScreen.kt"
      to: "InsightsViewModel"
      via: "collects uiState via viewModel.uiState.collectAsStateWithLifecycle()"
    - from: "RisksPaneScreen.kt"
      to: "RisksUiState"
      via: "extracts risks.alerts, risks.hasTransactions, hasEnoughHistory from uiState.risks"
    - from: "RisksPaneScreen.kt"
      to: "RisksAlertList"
      via: "passes alerts = uiState.risks.alerts to RisksAlertList"
    - from: "InsightsScreen.kt"
      to: "RisksPaneScreen"
      via: "page 1 of HorizontalPager calls RisksPaneScreen(modifier)"
---

<objective>
Create RisksPaneScreen root composable and error state, then integrate into InsightsScreen by replacing the stub.
Purpose: Wire the stateless composables from plan 01 to the ViewModel state, handle all visual hierarchy states (data/empty/error/loading).
Output: Complete Risks Pane integration ready for user testing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-risks-pane/030-UI-SPEC.md
@.planning/phases/30-risks-pane/030-01-PLAN.md
@.planning/REQUIREMENTS.md

<interfaces>
From InsightsScreen.kt (current stub):
```kotlin
@Composable
fun RisksPaneStub(modifier: Modifier = Modifier) {
    Box(
        modifier = modifier.fillMaxSize(),
        contentAlignment = Alignment.Center
    ) {
        Text(
            text = "Risks Pane — coming in Phase 30",
            fontSize = 16.sp
        )
    }
}
```

From InsightsViewModel.kt (state source):
```kotlin
@HiltViewModel
class InsightsViewModel @Inject constructor(
    private val repository: TransactionRepository,
    private val calculator: InsightsCalculator,
    @ApplicationContext private val context: Context
) : ViewModel() {
    val uiState: StateFlow<InsightsUiState> = ... // emits InsightsUiState
}
```

From InsightsUiState.kt:
```kotlin
data class InsightsUiState(
    val status: StatusUiState = StatusUiState(),
    val risks: RisksUiState = RisksUiState(),
    val trends: TrendsUiState = TrendsUiState(),
    val isLoading: Boolean = true,
    val hasEnoughHistory: Boolean = false,
    val currency: String = "INR"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create RisksPaneErrorState composable</name>
  <files>ui/insights/risks/RisksPaneErrorState.kt</files>
  <action>Create stateless RisksPaneErrorState composable per 030-UI-SPEC.md:

  **RisksPaneErrorState**: Accepts `onRetry: () -> Unit` parameter. Renders:
    - Full-screen centered Column (fillMaxSize, Alignment.Center)
    - Text: "Unable to load financial alerts. Pull to refresh or try again later." (exact copy from UI-SPEC error state)
    - Typography: bodyLarge (16sp, Regular 400)
    - Text color: MaterialTheme.colorScheme.error
    - Note: UI-SPEC specifies no retry button (display-only phase), but include `onRetry` callback for future use
    - If retry functionality needed later, can add Button with "Retry" text

  Use Material 3 components (Text, Column, Box) only. Follow Phase 29 empty state patterns (see StatusPaneEmptyState.kt for full-screen centered layout reference).
  </action>
  <verify>
    <automated>ls ui/insights/risks/RisksPaneErrorState.kt 2>&1 | wc -l</automated>
  </verify>
  <done>RisksPaneErrorState.kt exists, displays exact error copy from UI-SPEC, full-screen centered layout, accepts onRetry callback.</done>
</task>

<task type="auto">
  <name>task 2: create RisksPaneScreen root composable</name>
  <files>ui/insights/risks/RisksPaneScreen.kt</files>
  <action>Create RisksPaneScreen root composable that wires ViewModel state to stateless composables:

  **RisksPaneScreen**: Accepts `modifier: Modifier = Modifier` parameter. Renders:
    1. Collects state from ViewModel: `val uiState by viewModel.uiState.collectAsStateWithLifecycle()`
    2. Extracts RisksUiState: `val risksState = uiState.risks`
    3. Extracts hasEnoughHistory: `val hasEnoughHistory = uiState.hasEnoughHistory`

    4. Conditional rendering:
       - **If !risksState.hasTransactions**: Show RisksPaneEmptyState() (full-screen)
       - **Else (has transactions)**: Show scrollable Column with:
         a. RisksPaneHeader() at top
         b. Spacer(height = 32.dp = xl token) for layout gap
         c. RisksAlertList(alerts = risksState.alerts) - the alert cards
         d. If !hasEnoughHistory: RisksHistoryDisclaimer() with 16dp top padding

    5. Modifier: Apply modifier parameter, fillMaxSize()

  Use Material 3 components (Column, Spacer) and Compose runtime (collectAsStateWithLifecycle). Follow Phase 29 StatusPaneScreen.kt pattern for ViewModel wiring.
  Note: Error state handling can be added later when ViewModel exposes error state; for now focus on loading/data/empty states.
  </action>
  <verify>
    <automated>grep -c "fun RisksPaneScreen" ui/insights/risks/RisksPaneScreen.kt && grep -c "collectAsStateWithLifecycle" ui/insights/risks/RisksPaneScreen.kt</automated>
  </verify>
  <done>RisksPaneScreen.kt exists, wires to ViewModel via collectAsStateWithLifecycle, handles hasTransactions=false (empty state), renders header+list+disclaimer hierarchy correctly.</done>
</task>

<task type="auto">
  <name>task 3: update InsightsScreen.kt to use RisksPaneScreen</name>
  <files>ui/screens/InsightsScreen.kt</files>
  <action>Update InsightsScreen.kt to replace RisksPaneStub with actual RisksPaneScreen:

  1. **Remove** the RisksPaneStub composable function entirely (lines 72-83 in current file)

  2. **Update** the HorizontalPager page 1 to call RisksPaneScreen instead of RisksPaneStub:
     ```kotlin
     1 -> RisksPaneScreen(modifier = Modifier.fillMaxSize())
     ```

  3. **Add** import for RisksPaneScreen:
     ```kotlin
     import com.moneymanager.app.ui.insights.risks.RisksPaneScreen
     ```

  4. **Keep** TrendsPaneStub for now (Phase 31 will replace it later)

  Follow existing code style: same indentation (4 spaces), same import grouping, same formatting as current file.
  </action>
  <verify>
    <automated>grep -c "RisksPaneScreen(" ui/screens/InsightsScreen.kt && grep -c "RisksPaneStub" ui/screens/InsightsScreen.kt | grep -q "0" && echo "Stub removed"</automated>
  </verify>
  <done>InsightsScreen.kt updated, page 1 calls RisksPaneScreen(modifier), RisksPaneStub function removed, import added, TrendsPaneStub preserved for Phase 31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel → UI | StateFlow data flows from ViewModel to composables |
| Local UI | All data from local ViewModel, no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-30-04 | I | RisksPaneScreen | accept | Only renders user's own financial data from local ViewModel |
| T-30-05 | D | RisksPaneScreen | accept | No external dependencies, stateless composables with state hoisting |
| T-30-06 | E | Alert explanations | mitigate | Explanation text from InsightsCalculator (trusted), displayed via Text composable, no HTML rendering or web risks |
</threat_model>

<verification>
1. RisksPaneScreen.kt compiles without errors
2. InsightsScreen.kt compiles and page 1 shows RisksPaneScreen (not stub)
3. RisksPaneScreen handles all states: hasTransactions=false → EmptyState, hasTransactions=true → Header+List+Disclaimer
4. Alert ordering preserved (InsightsCalculator already sorts WARNING before INFO)
5. Max 3 alerts enforced (InsightsCalculator.take(3) already implemented)
6. Currency symbol (₹) appears in alert explanations (from InsightsCalculator)
7. UI-SPEC spacing: 32dp gap header-to-list (xl token), 16dp between cards (lg token)
</verification>

<success_criteria>
RisksPaneScreen created and wired to ViewModel, InsightsScreen updated to use real Risks Pane instead of stub, all 5 composables from plan 01 integrated correctly, handles empty/loaded/history-disclaimer states per RSK-01 to RSK-08.
</success_criteria>

<output>
After completion, create `.planning/phases/30-risks-pane/030-02-SUMMARY.md`
</output>
