---
phase: 40-user-facing-download-flow
plan: 03
type: execute
wave: 2
depends_on:
  - "40-01"
  - "40-02"
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/MoneyManagerNavHost.kt
autonomous: false
requirements:
  - HYBRID-05
  - HYBRID-06

must_haves:
  truths:
    - "AiDownloadConsentDialog appears in TransactionsScreen when showDownloadConsentDialog is true"
    - "Tapping 'Maybe Later' in the dialog calls viewModel.onDownloadPromptSuppressed() — dialog hides"
    - "Tapping 'Download (529 MB)' calls viewModel.onDownloadConsented() — dialog hides, banner appears"
    - "DownloadProgressBanner appears as first item in the TransactionsScreen LazyColumn while isDownloading is true"
    - "Banner scrolls away naturally (sticky=false) — it is a regular LazyColumn item with key 'download_banner'"
    - "Snackbar error messages for download failures use SnackbarDuration.Long"
    - "When download completes, the existing AI FAB group AnimatedVisibility reveals automatically (no new FAB code)"
    - "MoneyManagerNavHost still compiles cleanly after TransactionsViewModel constructor change"
    - "Human checkpoint: dialog appears on a LOCAL_DOWNLOADABLE device/emulator config"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt"
      provides: "Consent dialog trigger + download banner as first LazyColumn item + error snackbars"
      contains: "AiDownloadConsentDialog"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/MoneyManagerNavHost.kt"
      provides: "Compiles cleanly with updated TransactionsViewModel Hilt injection"
  key_links:
    - from: "TransactionsViewModel.showDownloadConsentDialog"
      to: "AiDownloadConsentDialog(onDownload, onMaybeLater)"
      via: "collectAsStateWithLifecycle() in TransactionsScreen"
      pattern: "showDownloadConsent"
    - from: "TransactionsViewModel.isDownloading"
      to: "DownloadProgressBanner(isVisible=...)"
      via: "collectAsStateWithLifecycle() in TransactionsScreen"
      pattern: "isDownloading"
    - from: "AiAvailabilityRepository.isAiAvailable"
      to: "TransactionsScreen isAiAssistAvailable parameter"
      via: "aiDraftViewModel.isAiAvailable in MoneyManagerNavHost"
      pattern: "isAiAssistAvailable"
---

<objective>
Wire the consent dialog and progress banner into TransactionsScreen. This is the only Wave 2 plan — it depends on both 40-01 (dialog composable + ViewModel consent state) and 40-02 (banner composable + ViewModel progress state). After this plan, the full Phase 40 flow is live: dialog shows on LOCAL_DOWNLOADABLE devices, download starts on consent, banner shows progress, FAB group auto-reveals on completion.

Purpose: Completes HYBRID-05 and HYBRID-06 end-to-end — user sees the full download consent + progress experience.
Output: TransactionsScreen.kt (dialog + banner wired), MoneyManagerNavHost.kt (Hilt graph clean), human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-user-facing-download-flow/40-01-SUMMARY.md
@.planning/phases/40-user-facing-download-flow/40-02-SUMMARY.md
@.planning/phases/40-user-facing-download-flow/40-UI-SPEC.md

<interfaces>
From TransactionsScreen.kt (current signature — additive changes only):
```kotlin
@Composable
fun TransactionsScreen(
    viewModel: TransactionsViewModel,
    initialType: String? = null,
    initialAccountId: Long? = null,
    initialStartDate: Long? = null,
    initialEndDate: Long? = null,
    initialGoalId: Long? = null,
    initialCategoryId: Long? = null,
    initialPeerId: Long? = null,
    isAiAssistAvailable: Boolean = false,
    onNavigateToAiDraftSms: () -> Unit = {},
    onNavigateToAiDraftReceipt: () -> Unit = {},
    onNavigateToAiDraftVoice: () -> Unit = {}
)
```

From TransactionsViewModel (added by Plans 40-01 and 40-02):
```kotlin
val showDownloadConsentDialog: StateFlow<Boolean>
val isDownloading: StateFlow<Boolean>
val downloadProgress: StateFlow<Float>
val downloadProgressCaption: StateFlow<String?>
val downloadProgressPercent: StateFlow<String>
val downloadError: StateFlow<String?>
fun onDownloadConsented()
fun onDownloadPromptSuppressed()
```

New composables (from Plans 40-01 and 40-02):
```kotlin
// com.moneymanager.app.ui.transactions.components.AiDownloadConsentDialog
@Composable fun AiDownloadConsentDialog(onDownload: () -> Unit, onMaybeLater: () -> Unit)

// com.moneymanager.app.ui.transactions.components.DownloadProgressBanner
@Composable fun DownloadProgressBanner(
    isVisible: Boolean, progress: Float, captionText: String?, percentText: String
)
```

From MoneyManagerNavHost.kt (current TransactionsScreen call site — lines 243-259):
```kotlin
val aiDraftViewModel = hiltViewModel<AiDraftViewModel>()
val isAiAvailable by aiDraftViewModel.isAiAvailable.collectAsState()
TransactionsScreen(
    viewModel = hiltViewModel(),  // TransactionsViewModel — Hilt injects all constructor params
    ...
    isAiAssistAvailable = isAiAvailable,
    ...
)
```
Note: Hilt injects TransactionsViewModel automatically via hiltViewModel(). The AiAvailabilityRepository added to TransactionsViewModel's constructor in Plan 40-01 must be provided by Hilt — verify that AiAvailabilityRepository is @Singleton and @Inject-annotated (it is, from AiAvailabilityRepository.kt). No manual NavHost changes required for the injection — Hilt handles it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AiDownloadConsentDialog and DownloadProgressBanner into TransactionsScreen</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt (full file — understand all existing collect calls, the LazyColumn structure starting at line ~550, the snackbarHostState usage if any)
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt (signature — from Plan 40-01)
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/DownloadProgressBanner.kt (signature — from Plan 40-02)
    - .planning/phases/40-user-facing-download-flow/40-UI-SPEC.md (Interaction States table + Snackbar Error Pattern section)
  </read_first>
  <action>
    All changes to TransactionsScreen.kt are STRICTLY ADDITIVE. Do not alter any existing parameter, logic, or composable.

    Step 1 — Add state collection near the top of the TransactionsScreen composable function body (after existing collectAsStateWithLifecycle calls):
    - val showDownloadConsent by viewModel.showDownloadConsentDialog.collectAsStateWithLifecycle()
    - val isDownloading by viewModel.isDownloading.collectAsStateWithLifecycle()
    - val downloadProgress by viewModel.downloadProgress.collectAsStateWithLifecycle()
    - val downloadProgressCaption by viewModel.downloadProgressCaption.collectAsStateWithLifecycle()
    - val downloadProgressPercent by viewModel.downloadProgressPercent.collectAsStateWithLifecycle()
    - val downloadError by viewModel.downloadError.collectAsStateWithLifecycle()

    Step 2 — Add a SnackbarHostState if not already present. Check: if the file already has a snackbarHostState (search existing code), reuse it. If absent, add val snackbarHostState = remember { SnackbarHostState() } and add SnackbarHost(snackbarHostState) to the Scaffold's snackbarHost slot.

    Step 3 — Add a LaunchedEffect for download error snackbars:
    LaunchedEffect(downloadError) { downloadError?.let { snackbarHostState.showSnackbar(it, duration = SnackbarDuration.Long) } }

    Step 4 — Add the dialog trigger. Immediately before the existing Scaffold call (or as a sibling composable alongside the Scaffold, not inside it), add:
    if (showDownloadConsent) { AiDownloadConsentDialog(onDownload = { viewModel.onDownloadConsented() }, onMaybeLater = { viewModel.onDownloadPromptSuppressed() }) }

    Step 5 — Add the DownloadProgressBanner as the first item in the LazyColumn. Locate the LazyColumn content block (at approximately line 550). Before the first date-grouped transaction items (before the empty-state item check), add:
    item(key = "download_banner") { DownloadProgressBanner(isVisible = isDownloading, progress = downloadProgress, captionText = downloadProgressCaption, percentText = downloadProgressPercent) }
    This item is placed BEFORE the if (visibleTransactions.isEmpty()) check.

    Step 6 — Add required imports:
    - import com.moneymanager.app.ui.transactions.components.AiDownloadConsentDialog
    - import com.moneymanager.app.ui.transactions.components.DownloadProgressBanner
    - import androidx.compose.material3.SnackbarHostState (if not already imported)
    - import androidx.compose.material3.SnackbarDuration (if not already imported)
    - import androidx.compose.material3.SnackbarHost (if not already imported)

    IMPORTANT: The existing AnimatedVisibility FAB group at lines 463-503 and the isAiAssistAvailable parameter are UNCHANGED. The FAB auto-reveal works through the existing AiAvailabilityRepository flow — no modifications needed.
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:compileDebugKotlin 2>&1 | tail -30</automated>
  </verify>
  <done>
    - grep -c "AiDownloadConsentDialog" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt returns >= 2 (import + usage)
    - grep -c "DownloadProgressBanner" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt returns >= 2 (import + usage)
    - grep -c "download_banner" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt returns 1
    - grep -c "SnackbarDuration.Long" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt returns >= 1
    - ./gradlew :app:compileDebugKotlin exits with BUILD SUCCESSFUL
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Hilt graph compiles and full test suite passes</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/MoneyManagerNavHost.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/MoneyManagerNavHost.kt (lines 240-260 — TransactionsScreen call site; confirm hiltViewModel() is used for TransactionsViewModel)
    - MoneyManager/app/src/main/java/com/moneymanager/data/repository/AiAvailabilityRepository.kt (confirm @Singleton + @Inject constructor — this is required for Hilt to auto-provide it into TransactionsViewModel)
  </read_first>
  <action>
    This task is primarily a verification task with one potential fix.

    Check 1 — MoneyManagerNavHost.kt: Confirm that TransactionsScreen is called with viewModel = hiltViewModel() (NOT with a manually constructed TransactionsViewModel). If it already uses hiltViewModel(), no change needed — Hilt will automatically inject the new AiAvailabilityRepository parameter.

    Check 2 — AiAvailabilityRepository.kt: Confirm that it is annotated @Singleton and has @Inject constructor. If it does NOT have @Inject constructor (meaning it is provided via @Provides in a Hilt module), that is also fine — Hilt will use the @Provides method. If neither annotation is present, that is a gap — add @Singleton @Inject constructor annotations.

    Check 3 — Run the full Hilt-aware compile: ./gradlew :app:kaptDebugKotlin to trigger KSP/KAPT annotation processing. If KSP errors appear related to AiAvailabilityRepository injection into TransactionsViewModel, fix them before proceeding.

    If MoneyManagerNavHost.kt requires no changes (most likely), document that in the task output but still run the compile check.

    Run the full unit test suite to confirm all plans' tests still pass together.
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:testDebugUnitTest 2>&1 | tail -30</automated>
  </verify>
  <done>
    - ./gradlew :app:testDebugUnitTest returns BUILD SUCCESSFUL
    - All DownloadConsentStateTest tests pass (7 tests from Plan 40-01)
    - All DownloadBannerStateTest tests pass (6 tests from Plan 40-02)
    - No KSP/KAPT errors in ./gradlew :app:kaptDebugKotlin output
    - If MoneyManagerNavHost.kt was unchanged, note that explicitly in the summary
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 40-01, 40-02, and 40-03 collectively deliver the full Phase 40 flow:
    - AiDownloadConsentDialog shown when device qualifies (tier=LOCAL_DOWNLOADABLE + not yet opted-in)
    - "Maybe Later" suppresses for session; re-shows on next launch
    - "Download (529 MB)" starts ModelDownloadService foreground download
    - DownloadProgressBanner appears as first item in TransactionsScreen LazyColumn while download runs
    - When download completes, existing AI FAB group (SMS/Receipt/Voice) auto-reveals
    - ModelDownloadService notification channel updated to "ai_model_download"
  </what-built>
  <how-to-verify>
    To verify the consent dialog trigger, one of these approaches works:

    Option A (emulator — easiest): Run the app and temporarily modify DeviceCapabilityManager to return AiBackend.LOCAL_MODEL + write "LOCAL_DOWNLOADABLE" to aiAvailabilityStatus. Then revert. Navigate to TransactionsScreen — the consent dialog should appear.

    Option B (inspect state): Add a temporary debug button or use Android Studio's DataStore inspector to set userOptedInAi=false and aiBackendTier="local_model" and isLocalModelDownloaded=false in the running app. Then navigate to TransactionsScreen.

    Option C (logcat): Launch the app and check logcat for "TransactionsViewModel" tag. Confirm showDownloadConsentDialog state emission.

    Core visual checks:
    1. Dialog title reads exactly "Enable On-Device AI?" (not "Enable On-Device AI" without the question mark)
    2. Privacy text reads "All AI processing happens entirely on your device. Your financial data is never sent to any server."
    3. Size text reads "Requires a one-time 529 MB download. Wi-Fi recommended."
    4. Confirm button reads "Download (529 MB)"
    5. Dismiss button reads "Maybe Later"
    6. Dialog icon is a sparkle/AutoAwesome icon in the primary color
    7. Tapping "Maybe Later" dismisses dialog without starting download (no banner appears)
    8. Banner (if testable): shows "Downloading AI model" title + LinearProgressIndicator + percentage

    IMPORTANT: Dialog will only show on a device/emulator where DeviceCapabilityManager writes LOCAL_DOWNLOADABLE. On AICore-equipped devices, the dialog will not show (correct behavior).
  </how-to-verify>
  <resume-signal>Type "approved" if visuals match spec, or describe any discrepancies for gap-closure planning</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TransactionsScreen → ViewModels | Screen reads consent and progress state; cannot bypass consent by calling ViewModel methods directly from outside the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Repudiation | TransactionsScreen dialog if-block | accept | Dialog is shown only when showDownloadConsentDialog StateFlow emits true — which requires tier=LOCAL_MODEL + !downloaded + !optedIn + !suppressed; no bypass path from the UI layer |
| T-40-06 | Denial of Service | DownloadProgressBanner LazyColumn item | mitigate | item(key = "download_banner") uses stable key to prevent list recomposition churn; AnimatedVisibility prevents layout shift from blocking user interaction |
</threat_model>

<verification>
Phase 40 success criteria (from ROADMAP.md):

SC1: User sees opt-in dialog disclosing "529 MB" download size and on-device privacy assurance before any download byte is transferred.
- Verified by: grep "529 MB" AiDownloadConsentDialog.kt returns >= 2; grep "on your device" AiDownloadConsentDialog.kt returns >= 1; grep "AiDownloadConsentDialog" TransactionsScreen.kt returns >= 2

SC2: Tapping "Maybe Later" dismisses dialog without initiating download; does not re-prompt within same app session; re-appears on next launch.
- Verified by: DownloadConsentStateTest Test 2 (session suppress); Test 6 (no DataStore write); onDownloadPromptSuppressed sets MutableStateFlow only

SC3: Tapping "Download (529 MB)" initiates download and dismisses dialog.
- Verified by: DownloadConsentStateTest Test 7 (setUserOptedInAi called); ModelDownloadService.start called in onDownloadConsented

SC4: User sees download progress in-app while download runs; app remains fully usable.
- Verified by: DownloadBannerStateTest Tests 1-6; banner as LazyColumn item (not overlay); DownloadProgressBanner isVisible=isDownloading

SC5: AI features become available automatically when download completes without app restart.
- Verified by: AiAvailabilityRepository.isAiAvailable flow (combines tier + isLocalModelDownloaded) emits true when ModelDownloadService calls setLocalModelDownloaded(true); existing AnimatedVisibility in TransactionsScreen lines 463-503 reacts to isAiAssistAvailable parameter fed from aiDraftViewModel.isAiAvailable in NavHost — this chain is already wired, no new code needed

Full automated gate: ./gradlew :app:testDebugUnitTest returns BUILD SUCCESSFUL
</verification>

<success_criteria>
- All 5 Phase 40 ROADMAP success criteria verified (see Verification section above)
- ./gradlew :app:testDebugUnitTest BUILD SUCCESSFUL with all 13 consent+banner tests passing
- ./gradlew :app:compileDebugKotlin BUILD SUCCESSFUL (no KSP errors)
- Human checkpoint approved
- HYBRID-05 and HYBRID-06 requirements satisfied end-to-end
</success_criteria>

<output>
After completion, create .planning/phases/40-user-facing-download-flow/40-03-SUMMARY.md
</output>
