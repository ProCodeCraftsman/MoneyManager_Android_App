---
phase: 40-user-facing-download-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt
  - MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadConsentStateTest.kt
autonomous: true
requirements:
  - HYBRID-05

must_haves:
  truths:
    - "Dialog is shown when aiBackendTier is LOCAL_MODEL, isLocalModelDownloaded is false, userOptedInAi is false, and session-suppress flag is false"
    - "Dialog is NOT shown when session-suppress flag is true (after 'Maybe Later' tap)"
    - "Dialog is NOT shown when isLocalModelDownloaded is true (tier is LOCAL_READY path)"
    - "Dialog is NOT shown when userOptedInAi is true"
    - "Tapping 'Maybe Later' sets in-memory session flag only — does not write to DataStore"
    - "Tapping 'Download (529 MB)' calls setUserOptedInAi(true) then ModelDownloadService.start()"
    - "scrim/back tap is treated identically to 'Maybe Later'"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt"
      provides: "AlertDialog composable with exact UI-SPEC copy"
      exports: ["AiDownloadConsentDialog"]
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt"
      provides: "showDownloadConsentDialog StateFlow, onDownloadConsented(), onDownloadPromptSuppressed()"
      contains: "_isDownloadPromptSuppressedForSession"
    - path: "MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadConsentStateTest.kt"
      provides: "Unit tests for HYBRID-05 dialog trigger logic"
  key_links:
    - from: "TransactionsViewModel.showDownloadConsentDialog"
      to: "AiDownloadConsentDialog"
      via: "collectAsState in TransactionsScreen (Plan 40-03)"
      pattern: "showDownloadConsentDialog"
    - from: "TransactionsViewModel.onDownloadConsented"
      to: "ModelDownloadService.start()"
      via: "viewModelScope.launch"
      pattern: "ModelDownloadService.start"
---

<objective>
Create the opt-in consent dialog composable (AiDownloadConsentDialog) and the ViewModel state that drives it. After this plan, the dialog exists as a standalone composable and TransactionsViewModel exposes the reactive boolean that determines whether to show it. No wiring into TransactionsScreen yet (that is Plan 40-03).

Purpose: Delivers HYBRID-05 — explicit user consent before any download byte is transferred.
Output: AiDownloadConsentDialog.kt, extended TransactionsViewModel.kt, DownloadConsentStateTest.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-user-facing-download-flow/40-RESEARCH.md
@.planning/phases/40-user-facing-download-flow/40-UI-SPEC.md

<interfaces>
From TransactionsViewModel.kt (current constructor, additive changes only):
```kotlin
@HiltViewModel
class TransactionsViewModel @Inject constructor(
    application: Application,
    private val transactionRepository: TransactionRepository,
    private val categoryRepository: CategoryRepository,
    private val accountRepository: AccountRepository,
    private val goalRepository: GoalRepository,
    private val peerContactRepository: PeerContactRepository,
    private val preferencesManager: PreferencesManager,
) : AndroidViewModel(application)
```

From PreferencesManager.kt (relevant flows and setters already present):
```kotlin
val aiBackendTier: Flow<String>            // emits "aicore" | "local_model" | "none"
val isLocalModelDownloaded: Flow<Boolean>
val userOptedInAi: Flow<Boolean>           // Flow<Boolean>, key "user_opted_in_ai"
suspend fun setUserOptedInAi(value: Boolean)
val selectedLocalModel: Flow<String>       // default "GEMMA3_1B"
fun getSelectedLocalModelSync(): String
```

From AiAvailabilityRepository.kt:
```kotlin
val aiBackendTier: Flow<AiBackend>         // emits AiBackend enum
```

From AiBackend enum (domain layer):
```kotlin
enum class AiBackend(val id: String) {
    AICORE("aicore"), LOCAL_MODEL("local_model"), NONE("none");
    companion object { fun fromId(id: String): AiBackend }
}
```

From ModelDownloadService.kt (companion object — use as-is):
```kotlin
companion object {
    fun start(context: Context, modelName: String)
    fun cancel(context: Context)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DownloadConsentStateTest — failing tests for HYBRID-05 trigger logic</name>
  <files>MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadConsentStateTest.kt</files>
  <read_first>
    - MoneyManager/app/src/test/java/com/moneymanager/data/ai/DeviceCapabilityManagerTest.kt (test class structure, mock setup, runTest pattern)
    - MoneyManager/app/src/main/java/com/moneymanager/data/repository/AiAvailabilityRepository.kt (AiState data class fields)
    - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/AiBackend.kt (enum values and fromId)
  </read_first>
  <behavior>
    - Test 1: showDownloadConsentDialog emits true when tier=LOCAL_MODEL, downloaded=false, optedIn=false, suppressed=false
    - Test 2: showDownloadConsentDialog emits false when tier=LOCAL_MODEL, downloaded=false, optedIn=false, suppressed=true (session suppress)
    - Test 3: showDownloadConsentDialog emits false when tier=LOCAL_MODEL, downloaded=true (model already downloaded)
    - Test 4: showDownloadConsentDialog emits false when tier=AICORE (wrong tier)
    - Test 5: showDownloadConsentDialog emits false when optedIn=true
    - Test 6: onDownloadPromptSuppressed() causes showDownloadConsentDialog to emit false
    - Test 7: onDownloadConsented() calls preferencesManager.setUserOptedInAi(true) — verified via mock
  </behavior>
  <action>
    Create DownloadConsentStateTest.kt in package com.moneymanager.data.ai. Use JUnit4 + mockito-kotlin (same imports as DeviceCapabilityManagerTest). The class under test is a logic helper or directly tests the combine expression — since TransactionsViewModel is an AndroidViewModel (requires Application), test the combine logic independently by creating a helper function or test the state derivation directly using turbine/runTest. 

    PREFERRED APPROACH: Extract the consent dialog show-condition into a package-private pure function showConsentDialog(tier: AiBackend, downloaded: Boolean, optedIn: Boolean, suppressed: Boolean): Boolean so it can be tested without instantiating TransactionsViewModel. Put this function in the TransactionsViewModel.kt file (package-private by default in Kotlin means internal package visibility; use internal or keep as top-level function in the same file).

    Test the function with all 7 behavior cases above using assertEquals. No mocks needed for the pure function tests (Test 1-5). Tests 6-7 test ViewModel methods — mock PreferencesManager using mock() and capture suspend call via runTest.

    This task creates the test file only. Tests MUST fail (RED) because TransactionsViewModel does not yet expose showDownloadConsentDialog or the helper function. Run: ./gradlew :app:testDebugUnitTest --tests "*.DownloadConsentStateTest" and confirm FAILURE.
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:testDebugUnitTest --tests "*.DownloadConsentStateTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Test file compiles but all 7 tests fail with "unresolved reference" or assertion failures — RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiDownloadConsentDialog composable + TransactionsViewModel download state</name>
  <files>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt,
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt
  </files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt (full current file — understand existing constructor, imports, all existing StateFlows before adding)
    - MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt (lines 93-175 — aiBackendTier, isLocalModelDownloaded, userOptedInAi, setUserOptedInAi, selectedLocalModel, getSelectedLocalModelSync)
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/aimodels/AiModelsScreen.kt (lines 462-530 — HuggingFaceLoginDialog AlertDialog structure to use as pattern)
    - .planning/phases/40-user-facing-download-flow/40-UI-SPEC.md (Component Inventory section 1 and Copywriting Contract — MANDATORY: use exact copy strings verbatim)
  </read_first>
  <action>
    Part A — AiDownloadConsentDialog.kt:

    Create MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt in package com.moneymanager.app.ui.transactions.components.

    Implement @Composable fun AiDownloadConsentDialog(onDownload: () -> Unit, onMaybeLater: () -> Unit) using M3 AlertDialog. Exact structure per UI-SPEC Component Inventory section 1:
    - onDismissRequest = onMaybeLater (scrim/back = Maybe Later)
    - icon slot: Icon(Icons.Default.AutoAwesome, contentDescription = "AI feature", tint = MaterialTheme.colorScheme.primary)
    - title slot: Text("Enable On-Device AI?")
    - text slot: Column { Text(privacyLine, style = MaterialTheme.typography.bodyMedium); Spacer(Modifier.height(8.dp)); Text(wifiLine, style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant) }
    - privacyLine = "All AI processing happens entirely on your device. Your financial data is never sent to any server."
    - wifiLine = "Requires a one-time 529 MB download. Wi-Fi recommended."
    - confirmButton: Button(onClick = onDownload) { Text("Download (529 MB)") }
    - dismissButton: TextButton(onClick = onMaybeLater) { Text("Maybe Later") }
    - No hardcoded Color() values — all tokens from MaterialTheme.colorScheme
    - No fenced code blocks in this action; implement exactly as described above

    Part B — TransactionsViewModel.kt additions (STRICTLY ADDITIVE):

    1. Add AiAvailabilityRepository to the @Inject constructor as a new parameter (per D-research: "TransactionsViewModel needs AiAvailabilityRepository injected"). Add after preferencesManager parameter with default annotation for Hilt. Import com.moneymanager.data.repository.AiAvailabilityRepository.

    2. Add private val _isDownloadPromptSuppressedForSession = MutableStateFlow(false) field.

    3. Add internal/package-level pure function shouldShowDownloadConsent(tier: AiBackend, downloaded: Boolean, optedIn: Boolean, suppressed: Boolean): Boolean = tier == AiBackend.LOCAL_MODEL && !downloaded && !optedIn && !suppressed — this is what the DownloadConsentStateTest tests.

    4. Add val showDownloadConsentDialog: StateFlow<Boolean> = combine(aiAvailabilityRepository.aiBackendTier, preferencesManager.isLocalModelDownloaded, preferencesManager.userOptedInAi, _isDownloadPromptSuppressedForSession) { tier, downloaded, optedIn, suppressed -> shouldShowDownloadConsent(tier, downloaded, optedIn, suppressed) }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), false)

    5. Add fun onDownloadConsented(): launches viewModelScope.launch { preferencesManager.setUserOptedInAi(true); val modelName = preferencesManager.getSelectedLocalModelSync(); ModelDownloadService.start(getApplication(), modelName) }

    6. Add fun onDownloadPromptSuppressed(): sets _isDownloadPromptSuppressedForSession.value = true

    Import additions needed: com.moneymanager.domain.ai.AiBackend, com.moneymanager.data.ai.ModelDownloadService. Do NOT write "Maybe Later" to DataStore — only the in-memory StateFlow flag.
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:testDebugUnitTest --tests "*.DownloadConsentStateTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AiDownloadConsentDialog.kt exists with exact copy strings (grep: "All AI processing happens entirely on your device", "529 MB", "Maybe Later", "Enable On-Device AI?")
    - TransactionsViewModel compiles with showDownloadConsentDialog StateFlow, onDownloadConsented(), onDownloadPromptSuppressed() present
    - All 7 DownloadConsentStateTest tests pass (GREEN)
    - No DataStore write on "Maybe Later" path (verify: onDownloadPromptSuppressed only sets MutableStateFlow, no suspend call)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → ViewModel | User confirms or defers consent; must not initiate download without confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Repudiation | TransactionsViewModel.onDownloadConsented | mitigate | setUserOptedInAi(true) is called before ModelDownloadService.start() — if process is killed between the two, next launch re-shows dialog (safe recovery); download never starts without explicit tap |
| T-40-02 | Elevation of Privilege | AiDownloadConsentDialog onDismissRequest | accept | scrim/back is treated as Maybe Later (no download); no privilege boundary crossed; correct Android dialog pattern |
</threat_model>

<verification>
Run full DownloadConsentStateTest suite and spot-check:
- grep -c "Maybe Later" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt must return >= 1
- grep -c "529 MB" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt must return >= 2 (dialog body + button)
- grep -c "on your device" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/AiDownloadConsentDialog.kt must return >= 1
- grep -c "showDownloadConsentDialog" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt must return >= 1
- grep -c "setUserOptedInAi" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt must return >= 1
- ./gradlew :app:testDebugUnitTest --tests "*.DownloadConsentStateTest" returns BUILD SUCCESSFUL
</verification>

<success_criteria>
- AiDownloadConsentDialog.kt compiles with all exact copy strings from UI-SPEC Copywriting Contract
- TransactionsViewModel exposes showDownloadConsentDialog StateFlow combining 4 inputs
- onDownloadConsented() calls setUserOptedInAi(true) then ModelDownloadService.start()
- onDownloadPromptSuppressed() only sets in-memory MutableStateFlow (no DataStore write)
- 7 unit tests in DownloadConsentStateTest pass
- No hardcoded Color() values anywhere in AiDownloadConsentDialog.kt
- Hilt graph still compiles (no KSP errors from adding AiAvailabilityRepository to constructor)
</success_criteria>

<output>
After completion, create .planning/phases/40-user-facing-download-flow/40-01-SUMMARY.md
</output>
