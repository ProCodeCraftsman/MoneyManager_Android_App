---
phase: 40-user-facing-download-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/DownloadProgressBanner.kt
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt
  - MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt
  - MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadBannerStateTest.kt
autonomous: true
requirements:
  - HYBRID-06

must_haves:
  truths:
    - "Banner is visible when localModelDownloadProgress is between 0 (exclusive) and 1 (exclusive)"
    - "Banner is hidden when localModelDownloadProgress is 0f or 1f"
    - "LinearProgressIndicator is indeterminate when totalBytes is 0L (totalBytes not yet known)"
    - "LinearProgressIndicator is determinate when totalBytes > 0L, showing receivedBytes/totalBytes ratio"
    - "Caption text follows format: 'X.X / YYY MB (NN%)' with optional speed and ETA appended"
    - "Banner enter animation is fadeIn() + expandVertically(); exit is fadeOut() + shrinkVertically()"
    - "ModelDownloadService CHANNEL_ID is 'ai_model_download' (not 'model_download')"
    - "Notification channel human name is 'AI Model Download'"
    - "Banner snackbar errors use SnackbarDuration.Long"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/DownloadProgressBanner.kt"
      provides: "AnimatedVisibility Card composable with LinearProgressIndicator and caption"
      exports: ["DownloadProgressBanner"]
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt"
      provides: "isDownloading, downloadProgress, downloadProgressCaption, downloadProgressPercent, downloadError StateFlows"
      contains: "isDownloading"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt"
      provides: "Updated CHANNEL_ID constant"
      contains: "ai_model_download"
    - path: "MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadBannerStateTest.kt"
      provides: "Unit tests for HYBRID-06 banner visibility logic"
  key_links:
    - from: "PreferencesManager.localModelDownloadProgress"
      to: "TransactionsViewModel.isDownloading"
      via: "map { it > 0f && it < 1f }.stateIn()"
      pattern: "isDownloading"
    - from: "TransactionsViewModel.downloadProgress"
      to: "DownloadProgressBanner progress parameter"
      via: "collectAsState in TransactionsScreen (Plan 40-03)"
      pattern: "downloadProgress"
    - from: "ModelDownloadService.CHANNEL_ID"
      to: "NotificationChannel constructor"
      via: "createNotificationChannel() in onCreate()"
      pattern: "ai_model_download"
---

<objective>
Create the DownloadProgressBanner composable and the ViewModel state that drives it. Also fix the ModelDownloadService notification channel ID to match the UI-SPEC (from "model_download" to "ai_model_download"). These two plans (40-01 and 40-02) are independent and can execute in parallel in Wave 1.

Purpose: Delivers HYBRID-06 — persistent in-app progress indicator during download; app remains usable.
Output: DownloadProgressBanner.kt, extended TransactionsViewModel.kt (download progress StateFlows), ModelDownloadService.kt (channel ID fix), DownloadBannerStateTest.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-user-facing-download-flow/40-RESEARCH.md
@.planning/phases/40-user-facing-download-flow/40-UI-SPEC.md

<interfaces>
From PreferencesManager.kt (relevant progress flows — all already exist):
```kotlin
val localModelDownloadProgress: Flow<Float>    // 0f=idle/none, 1f=complete, 0<p<1=in-progress
val localModelDownloadReceived: Flow<Long>      // bytes received
val localModelDownloadTotal: Flow<Long>         // total bytes (0L when unknown)
val localModelDownloadSpeed: Flow<Long>         // bytes/sec
val isLocalModelDownloaded: Flow<Boolean>
```

From AiModelsScreen.kt (buildDownloadProgressText pattern to replicate — DO NOT import cross-package):
```kotlin
// Signature to replicate in TransactionsViewModel or a local utility:
private fun buildDownloadProgressText(
    receivedBytes: Long, totalBytes: Long,
    bytesPerSecond: Long, remainingMs: Long
): String {
    val receivedMb = "%.1f".format(receivedBytes / 1_000_000.0)
    val totalMb = "%.0f".format(totalBytes / 1_000_000.0)
    val pct = (receivedBytes * 100 / totalBytes).toInt()
    // speed and ETA optional appends
    return "$receivedMb / $totalMb MB ($pct%)" + optional speed + optional ETA
}
```

From ModelDownloadService.kt (line 39 — change this constant):
```kotlin
private const val CHANNEL_ID = "model_download"  // CHANGE to "ai_model_download"
```
Note: Line ~208 creates the NotificationChannel — also update human name to "AI Model Download".

From AiModelsScreen.kt DownloadProgressBanner pattern (lines 390-432):
```kotlin
// LinearProgressIndicator at 4.dp height with RoundedCornerShape(2.dp):
LinearProgressIndicator(
    progress = { localModelDownloadProgress },
    modifier = Modifier.fillMaxWidth().height(4.dp).clip(RoundedCornerShape(2.dp))
)
// Indeterminate when totalBytes == 0L:
LinearProgressIndicator(
    modifier = Modifier.fillMaxWidth().height(4.dp).clip(RoundedCornerShape(2.dp))
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DownloadBannerStateTest — failing tests for HYBRID-06 banner visibility</name>
  <files>MoneyManager/app/src/test/java/com/moneymanager/data/ai/DownloadBannerStateTest.kt</files>
  <read_first>
    - MoneyManager/app/src/test/java/com/moneymanager/data/ai/DeviceCapabilityManagerTest.kt (import style, runTest usage)
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/aimodels/ModelDownloadProgress.kt (ModelDownloadProgress data class — progress computed property)
  </read_first>
  <behavior>
    - Test 1: isDownloading emits true when localModelDownloadProgress = 0.47f
    - Test 2: isDownloading emits false when localModelDownloadProgress = 0f (idle)
    - Test 3: isDownloading emits false when localModelDownloadProgress = 1f (complete)
    - Test 4: captionText is null/empty when totalBytes = 0L (indeterminate state)
    - Test 5: captionText contains "/ 529 MB" when totalBytes = 529_000_000L
    - Test 6: percentText is "47%" when progress = 0.47f
  </behavior>
  <action>
    Create DownloadBannerStateTest.kt in package com.moneymanager.data.ai. Test the banner visibility predicate (isDownloading = progress > 0f && progress < 1f) as a pure function or via the same shouldShowDownloadBanner helper approach used in Plan 40-01.

    For caption text tests (Tests 4-5): create or reference a pure function buildBannerCaptionText(receivedBytes: Long, totalBytes: Long, bytesPerSecond: Long): String? that returns null when totalBytes == 0L.

    For percent text (Test 6): test a pure function bannerPercentText(progress: Float): String that returns "${(progress * 100).toInt()}%".

    Tests MUST fail (RED) because the functions/ViewModel state do not yet exist. Confirm with: ./gradlew :app:testDebugUnitTest --tests "*.DownloadBannerStateTest" returning FAILED.
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:testDebugUnitTest --tests "*.DownloadBannerStateTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Test file compiles but all 6 tests fail — RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DownloadProgressBanner composable + TransactionsViewModel progress state + notification channel fix</name>
  <files>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/DownloadProgressBanner.kt,
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt,
    MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt
  </files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt (full current file AFTER Plan 40-01 changes — note existing imports and all added fields from 40-01)
    - MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt (lines 37-45 for CHANNEL_ID constant; lines 200-215 for createNotificationChannel — check actual line numbers by reading)
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/aimodels/AiModelsScreen.kt (lines 380-460 for BackendStatusCard and buildDownloadProgressText — replicate exactly, do not import cross-package)
    - .planning/phases/40-user-facing-download-flow/40-UI-SPEC.md (Component Inventory section 2 — full DownloadProgressBanner spec)
  </read_first>
  <action>
    Part A — ModelDownloadService.kt notification channel fix (one-line constant change):
    Change the private const val CHANNEL_ID from "model_download" to "ai_model_download". Also update the human-readable name passed to NotificationChannel(...) from whatever it currently is to "AI Model Download". Importance stays IMPORTANCE_LOW. This is the only change to ModelDownloadService.kt.

    Part B — TransactionsViewModel.kt additions (STRICTLY ADDITIVE — do not break Plan 40-01 additions):

    1. Add internal fun buildBannerCaptionText(receivedBytes: Long, totalBytes: Long, bytesPerSecond: Long): String? — returns null when totalBytes == 0L. When totalBytes > 0L: format as "X.X / YYY MB (NN%)" where X.X = "%.1f".format(receivedBytes/1_000_000.0) and YYY = "%.0f".format(totalBytes/1_000_000.0) and NN = (receivedBytes*100/totalBytes).toInt(). Append " — N MB/s" if bytesPerSecond > 1_000_000L (format as "%.1f MB/s"), " — NNN KB/s" if > 1_000L, " — NNN B/s" otherwise. No ETA for this implementation (speed-only caption is sufficient for HYBRID-06).

    2. Add internal fun buildBannerPercentText(progress: Float): String = "${(progress * 100).toInt()}%"

    3. Add val isDownloading: StateFlow<Boolean> = preferencesManager.localModelDownloadProgress.map { it > 0f && it < 1f }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), false)

    4. Add val downloadProgress: StateFlow<Float> = preferencesManager.localModelDownloadProgress.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), 0f)

    5. Add val downloadProgressCaption: StateFlow<String?> = combine(preferencesManager.localModelDownloadReceived, preferencesManager.localModelDownloadTotal, preferencesManager.localModelDownloadSpeed) { received, total, speed -> buildBannerCaptionText(received, total, speed) }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), null)

    6. Add val downloadProgressPercent: StateFlow<String> = preferencesManager.localModelDownloadProgress.map { buildBannerPercentText(it) }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), "0%")

    7. Add val downloadError: StateFlow<String?> — derived from a new MutableStateFlow that ModelDownloadService errors would write. Since ModelDownloadService is a foreground service (cannot easily callback into ViewModel), instead observe: preferencesManager.localModelDownloadProgress.map { p -> if (p == 0f && /* was downloading */ false) "Download failed. Open AI settings to retry." else null } — SIMPLIFICATION: for Phase 40, expose a _downloadError MutableStateFlow<String?> = MutableStateFlow(null) and a val downloadError: StateFlow<String?> = _downloadError.asStateFlow(). A future gap-closure phase can wire the error signal from ModelDownloadService.

    Part C — DownloadProgressBanner.kt:
    Create in package com.moneymanager.app.ui.transactions.components.

    Implement @Composable fun DownloadProgressBanner(isVisible: Boolean, progress: Float, captionText: String?, percentText: String). Structure per UI-SPEC Component Inventory section 2:
    - AnimatedVisibility(visible = isVisible, enter = fadeIn() + expandVertically(), exit = fadeOut() + shrinkVertically())
    - Card(modifier = Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 8.dp), colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surfaceVariant))
    - Column(Modifier.padding(16.dp)) containing:
      - Row(verticalAlignment = Alignment.CenterVertically): Icon(Icons.Default.CloudDownload, contentDescription = null, tint = MaterialTheme.colorScheme.primary, modifier = Modifier.size(16.dp)); Spacer(Modifier.width(4.dp)); Text("Downloading AI model", style = MaterialTheme.typography.labelMedium, fontWeight = FontWeight.Medium, modifier = Modifier.weight(1f)); Text(percentText, style = MaterialTheme.typography.labelMedium, color = MaterialTheme.colorScheme.primary)
      - Spacer(Modifier.height(8.dp))
      - if (progress > 0f): LinearProgressIndicator(progress = { progress }, modifier = Modifier.fillMaxWidth().height(4.dp).clip(RoundedCornerShape(2.dp))) ELSE LinearProgressIndicator(modifier = Modifier.fillMaxWidth().height(4.dp).clip(RoundedCornerShape(2.dp)))
      - if (captionText != null): Spacer(Modifier.height(4.dp)); Text(captionText, style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant)
    - NO hardcoded Color() values
    - Required imports: AnimatedVisibility, expandVertically, shrinkVertically, fadeIn, fadeOut from compose.animation; Card, CardDefaults, LinearProgressIndicator from material3; Icons.Default.CloudDownload from material.icons.filled; RoundedCornerShape from foundation.shape; FontWeight from ui.text.font; clip from ui.draw
  </action>
  <verify>
    <automated>cd MoneyManager && ./gradlew :app:testDebugUnitTest --tests "*.DownloadBannerStateTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DownloadProgressBanner.kt exists with AnimatedVisibility, LinearProgressIndicator at height(4.dp).clip(RoundedCornerShape(2.dp)), "Downloading AI model" text
    - grep "ai_model_download" MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt returns at least 2 matches (CHANNEL_ID constant + NotificationChannel call)
    - grep "AI Model Download" MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt returns 1 match
    - TransactionsViewModel compiles with isDownloading, downloadProgress, downloadProgressCaption, downloadProgressPercent StateFlows
    - All 6 DownloadBannerStateTest tests pass (GREEN)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ModelDownloadService → PreferencesManager | Service writes progress; ViewModel reads it reactively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Denial of Service | DownloadProgressBanner LazyColumn item | accept | Banner is a first LazyColumn item (scrolls away), not an overlay; no touch interception possible |
| T-40-04 | Repudiation | ModelDownloadService channel ID change | accept | Updating CHANNEL_ID from "model_download" to "ai_model_download" creates a new Android notification channel; old channel persists silently until user clears app notification settings — no data loss, no security concern |
</threat_model>

<verification>
- grep -c "ai_model_download" MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt must return >= 2
- grep -c "model_download" MoneyManager/app/src/main/java/com/moneymanager/data/ai/ModelDownloadService.kt must return 0 (old ID removed)
- grep -c "Downloading AI model" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/components/DownloadProgressBanner.kt must return 1
- grep -c "isDownloading" MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsViewModel.kt must return >= 1
- ./gradlew :app:testDebugUnitTest --tests "*.DownloadBannerStateTest" returns BUILD SUCCESSFUL
- ./gradlew :app:testDebugUnitTest returns BUILD SUCCESSFUL (full suite green)
</verification>

<success_criteria>
- DownloadProgressBanner.kt compiles; no Color() literals; uses surfaceVariant card + primary indicator
- ModelDownloadService.CHANNEL_ID = "ai_model_download"; channel human name = "AI Model Download"
- TransactionsViewModel has isDownloading, downloadProgress, downloadProgressCaption, downloadProgressPercent StateFlows
- buildBannerCaptionText returns null when totalBytes=0L and "X.X / YYY MB (NN%)" when totalBytes > 0
- All 6 DownloadBannerStateTest tests pass
- Full test suite remains green (no regressions from Plan 40-01 additions)
</success_criteria>

<output>
After completion, create .planning/phases/40-user-facing-download-flow/40-02-SUMMARY.md
</output>
