---
phase: 24-scroll-to-top-global
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/components/ScrollToTopModifier.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "ScrollToTop modifier works with any LazyColumn via lazyListState injection"
    - "Button appears after scrolling past 50dp threshold"
    - "Button positioned center-bottom with 16dp margin"
    - "Applied to all 13 scrollable screens"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/components/ScrollToTopModifier.kt"
      provides: "Reusable scroll-to-top modifier for LazyColumn"
      exports: ["Modifier.scrollToTop"]
  key_links:
    - from: "ScrollToTopModifier.kt"
      to: "LazyColumn"
      via: "lazyListState parameter"
      pattern: "LazyListState.*scrollToTop"
---

<objective>
Create a global reusable scroll-to-top modifier that wraps LazyColumn and shows a floating button when users scroll past 50dp threshold.

Purpose: Users can quickly return to the top of any scrollable list by tapping the floating button
Output: ScrollToTopModifier.kt (reusable modifier) applied to all 13 screens
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-scroll-to-top-global/24-CONTEXT.md
@.planning/phases/24-scroll-to-top-global/24-UI-SPEC.md

# Scroll detection pattern from TransactionsScreen (lines 149-151):
# val isScrolled by remember {
#     derivedStateOf { lazyListState.firstVisibleItemIndex > 0 || lazyListState.firstVisibleItemScrollOffset > 50 }
# }
</context>

<interfaces>
<!-- Key types and contracts needed -->
<!-- From: androidx.compose.foundation.lazy.LazyListState -->
```kotlin
class LazyListState(
    val firstVisibleItemIndex: Int,
    val firstVisibleItemScrollOffset: Int
)
```

<!-- Usage pattern in screens -->
```kotlin
val lazyListState = rememberLazyListState()
val showScrollToTop by remember {
    derivedStateOf { lazyListState.firstVisibleItemIndex > 0 || lazyListState.firstVisibleItemScrollOffset > 50 }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ScrollToTopModifier composable</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/components/ScrollToTopModifier.kt</files>
  <behavior>
    - Test 1: Modifier accepts LazyListState parameter and threshold (default 50dp)
    - Test 2: Returns Modifier that renders FAB when scrolled past threshold
    - Test 3: FAB uses Icons.Default.KeyboardArrowUp with 48dp circular size
    - Test 4: FAB positioned center-bottom with 16dp bottom margin
    - Test 5: Click scrolls to index 0 with animateScrollToItem(0)
    - Test 6: Fade in/out animation (300ms enter, 200ms exit)
  </behavior>
  <action>
Create ScrollToTopModifier.kt with:

1. @Composable function scrollToTop(
   lazyListState: LazyListState,
   threshold: Dp = 50.dp,
   bottomMargin: Dp = 16.dp
): Modifier

2. Implementation:
   - Use derivedStateOf to detect scroll beyond threshold
   - Box with fillMaxSize as parent modifier (to position FAB over content)
   - SmallFloatingActionButton with Icons.Default.KeyboardArrowUp
   - Circular 48dp size (cornerRadius = 28dp)
   - MaterialTheme.colorScheme.secondaryContainer background
   - MaterialTheme.colorScheme.onSecondaryContainer icon tint
   - 6dp elevation
   - Center horizontally, 16dp from bottom
   - AnimatedVisibility with fadeIn/fadeOut (300ms/200ms)
   - onClick: lazyListState.animateScrollToItem(0)

3. Use animateFloatAsState for alpha if needed for smooth fade
  </action>
  <verify>
<automated>MISSING — No existing test file for UI modifiers, verify manually via build</automated>
  </verify>
  <done>ScrollToTopModifier.kt exists with scrollToTop function exported</done>
</task>

<task type="auto">
  <name>Task 2: Apply scrollToTop modifier to all 13 screens</name>
  <files>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/DashboardScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/AccountsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/BudgetsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/ReportsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/RecurringListScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/GoalsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/CategoriesScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TagsScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TemplatesScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/PeerListScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/BorrowLendScreen.kt
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/SettingsScreen.kt
  </files>
  <action>
For each of the 13 screens, update the LazyColumn modifier:

1. Add import for scrollToTop modifier:
   import com.moneymanager.app.ui.components.scrollToTop

2. Wrap the LazyColumn modifier with .scrollToTop(lazyListState):
   
   BEFORE:
   LazyColumn(
       modifier = Modifier
           .fillMaxSize()
           .padding(padding)
   )
   
   AFTER:
   LazyColumn(
       modifier = Modifier
           .fillMaxSize()
           .padding(padding)
           .scrollToTop(lazyListState)
   )

3. For screens that already have lazyListState (like TransactionsScreen), 
   simply add .scrollToTop(lazyListState) to the existing modifier chain.

4. For screens without lazyListState, add:
   - val lazyListState = rememberLazyListState()
   - Add .scrollToTop(lazyListState) to the LazyColumn modifier
  </action>
  <verify>
<automated>./gradlew assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <done>All 13 screens compile with scrollToTop modifier applied</done>
</task>

</tasks>

<verification>
- ScrollToTopModifier.kt exports scrollToTop function
- All 13 screens have LazyColumn with scrollToTop applied
- Build succeeds without errors
</verification>

<success_criteria>
1. ScrollToTop modifier works with any LazyColumn via lazyListState injection
2. Button appears after scrolling past 50dp threshold
3. Button positioned center-bottom with 16dp margin
4. Applied to all 13 scrollable screens
</success_criteria>

<output>
After completion, create `.planning/phases/24-scroll-to-top-global/{phase}-01-SUMMARY.md`
</output>