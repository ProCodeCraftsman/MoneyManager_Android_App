---
phase: 19-transactions-ui-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt"
autonomous: true
requirements:
  - TXUI-01
  - TXUI-02
  - TXUI-03
  - TXUI-04
  - TXUI-05

must_haves:
  truths:
    - "Status bar color matches header background color"
    - "Sticky date headers have visible elevation (shadow + spacing)"
    - "Search placeholder text is centered vertically"
    - "Filter chips height matches text field height"
    - "Active filter indicator has lightweight styling (30% opacity)"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt"
      provides: "UI corrections to TransactionsScreen"
      min_lines: 600
  key_links:
    - from: "TransactionsScreen.kt"
      to: "WindowCompat API"
      via: "statusBar color change"
      pattern: "WindowCompat.*statusBar"
    - from: "TransactionsScreen.kt date header"
      to: "Surface/Scaffold"
      via: "elevation modifier"
      pattern: "shadowElevation.*2\\.dp"
---

<objective>
Fix visual/UI issues in TransactionsScreen: status bar color, date header elevation, search placeholder centering, filter chip heights, and active filter indicator styling.

**Purpose:** Improve UI readability and visual consistency

**Output:** Updated TransactionsScreen.kt with all UI corrections applied
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-transactions-ui-fixes/19-CONTEXT.md
@.planning/phases/19-transactions-ui-fixes/19-RESEARCH.md
@.planning/STATE.md

# Existing code - TransactionsScreen.kt
MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix status bar color to match header</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt (lines ~247-260 for header/Scaffold)
    - Check existing WindowCompat imports or similar status bar handling in the codebase
  </read_first>
  <action>
Add status bar color matching using WindowCompat API. At the top of TransactionsScreen composable or in the header section, add:

1. Import: `androidx.core.view.WindowCompat`
2. Get window from LocalContext.current as Activity
3. Set status bar appearance to match header background:

```kotlin
val window = (LocalContext.current as Activity).window
WindowCompat.getInsetsController(window, window.decorView).isAppearanceLightStatusBars = false
// Or set to match your header's dark/light appearance
```

Per D-01: Match status bar color to header background using WindowCompat API.
  </action>
  <acceptance_criteria>
    - grep "WindowCompat.*statusBar" TransactionsScreen.kt returns match
    - Or grep "isAppearanceLightStatusBars" returns match
    - Build compiles without error
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>Status bar color matches header background</done>
</task>

<task type="auto">
  <name>Task 2: Add elevation to sticky date headers</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt (lines ~498-564 for date header sticky rendering)
    - Check existing date header implementation with collapsedDates pattern
  </read_first>
  <action>
Add both shadow elevation AND vertical padding to sticky date headers (per D-02, D-02a, D-02b):

In the stickyHeader block where date headers are rendered (look for collapsedDates usage):

1. Add shadow elevation: Add `shadowElevation = 2.dp` to the Surface or modifier
2. Add vertical padding: Add `padding(vertical = 6.dp)` or modifier with vertical spacing

Example pattern:
```kotlin
Surface(
    modifier = Modifier
        .fillMaxWidth()
        .shadowElevation(2.dp)
        .padding(vertical = 6.dp),
    tonalElevation = 1.dp
) {
    // Date header content
}
```

Current state has 1.dp tonalElevation. Per D-02: Add BOTH elevation and padding.
  </action>
  <acceptance_criteria>
    - grep "shadowElevation.*2\\.dp" TransactionsScreen.kt returns match
    - grep "padding.*6\\.dp\\|padding.*vertical" returns match
    - Build compiles without error
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>Sticky date headers have visible shadow and spacing gap</done>
</task>

<task type="auto">
  <name>Task 3: Center search placeholder text vertically</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt (search bar section, typically around search field)
    - Check existing BasicTextField or TextField implementation
  </read_first>
  <action>
Center the search placeholder text vertically within the search bar height (per D-03):

Find the search field (BasicTextField) and apply vertical centering:

1. Use `contentAlignment = Alignment.CenterVertically` on BasicTextField if available
2. Or add modifier to text element: `.align(Alignment.CenterVertically)`

Example:
```kotlin
BasicTextField(
    value = searchQuery,
    onValueChange = { searchQuery = it },
    modifier = Modifier
        .fillMaxWidth()
        .heightIn(min = 48.dp), // Ensure height
    textStyle = LocalTextStyle.current.copy(
        alignment = Alignment.CenterHorizontally
    ),
    // For vertical centering of placeholder:
    decorationBox = { innerTextField ->
        Box(
            modifier = Modifier.fillMaxSize(),
            contentAlignment = Alignment.CenterStart // or CenterVertically
        ) {
            innerTextField()
        }
    }
)
```

Per D-03: Center placeholder text vertically within current search bar height.
Per D-04: Keep search icon position as-is (right side).
  </action>
  <acceptance_criteria>
    - grep "CenterVertically\\|contentAlignment" TransactionsScreen.kt in search section returns match
    - Build compiles without error
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>Search placeholder text is centered vertically</done>
</task>

<task type="auto">
  <name>Task 4: Match filter chip height to text field, fix indicator styling</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt (filter chips section)
    - Check filter chip implementation and active indicator styling
  </read_first>
  <action>
Two fixes (per D-05 and D-06):

**D-05: Match filter chip height to text field height**
- Find the FilterChip or similar composable
- Adjust height to match search bar text field height
- Add `.height()` modifier or use same minHeight as search field

**D-06: Lightweight active filter indicator**
- Set active filter background to 30% opacity
- Reduce horizontal padding from default

Example:
```kotlin
FilterChip(
    selected = isSelected,
    onClick = { ... },
    modifier = Modifier.height(48.dp), // Match text field height
    colors = FilterChipDefaults.filterChipColors(
        selectedContainerColor = MaterialTheme.colorScheme.primary.copy(alpha = 0.3f) // 30% opacity
    ),
    contentPadding = PaddingValues(horizontal = 8.dp) // Reduced padding
)
```
  </action>
  <acceptance_criteria>
    - grep "FilterChip.*height\\|height.*48" TransactionsScreen.kt returns match
    - grep "0\\.3f\\|alpha.*0\\.3" returns match (30% opacity)
    - grep "contentPadding.*horizontal.*8" returns match (reduced padding)
    - Build compiles without error
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>Filter chips match height and have lightweight active indicator</done>
</task>

</tasks>

<verification>
All UI corrections applied to TransactionsScreen.kt:
1. Status bar color matches header - WindowCompat used
2. Date headers have shadowElevation(2.dp) + padding(vertical=6.dp)
3. Search placeholder centered vertically
4. Filter chips have matching height (48dp) 
5. Active filter has 30% opacity + reduced padding
</verification>

<success_criteria>
[ ✓ ] Status bar color matches header background color
[ ✓ ] Sticky date headers have visible elevation (shadow + spacing)
[ ✓ ] Search placeholder text is centered vertically
[ ✓ ] Filter chips height matches text field height
[ ✓ ] Active filter indicator has lightweight styling (30% opacity)
[ ✓ ] Build compiles without error
</success_criteria>

<output>
After completion, create `.planning/phases/19-transactions-ui-fixes/19-01-SUMMARY.md`
</output>