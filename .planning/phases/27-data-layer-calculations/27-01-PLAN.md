---
phase: 27-data-layer-calculations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/moneymanager/app/ui/screens/InsightsCalculator.kt
  - app/src/main/java/com/moneymanager/app/ui/screens/InsightsUiState.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "InsightsCalculator.compute() runs without Android runtime — pure Kotlin object"
    - "Split child transactions (isSplitChild=true) excluded from all aggregations"
    - "All 7 monetary figures computed from same current-month date window"
  artifacts:
    - path: "app/src/main/java/com/moneymanager/app/ui/screens/InsightsCalculator.kt"
      provides: "Pure Kotlin calculation logic"
      min_lines: 100
    - path: "app/src/main/java/com/moneymanager/app/ui/screens/InsightsUiState.kt"
      provides: "UI state contracts for all 3 panes"
      contains: "data class"
  key_links:
    - from: "InsightsCalculator.kt"
      to: "TransactionEntity.kt"
      via: "accepts List<TransactionEntity>"
      pattern: "List<TransactionEntity>"
    - from: "InsightsUiState.kt"
      to: "InsightsCalculator.kt"
      via: "return type of compute()"
      pattern: "InsightsData"
---

<objective>
Create the InsightsCalculator pure Kotlin object and InsightsUiState data contracts.

Purpose: Establish the calculation engine and UI state contracts that phases 28-31 will consume. This is pure business logic with no Android dependencies.

Output: 
- InsightsCalculator.kt — pure calculation object
- InsightsUiState.kt — state contracts for Status/Risks/Trends panes
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Android projec\.planning\PROJECT.md
@D:\Android projec\.planning\ROADMAP.md
@D:\Android projec\.planning\STATE.md
@D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\data\entity\TransactionEntity.kt
@D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\data\dao\TransactionDao.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InsightsUiState data contracts</name>
  <files>app/src/main/java/com/moneymanager/app/ui/screens/InsightsUiState.kt</files>
  <action>
    Create data classes for all UI states needed by the 3 panes. Per STATE.md decision: InsightsUiState decomposed into StatusUiState, RisksUiState, TrendsUiState sub-states.
    
    Define:
    - `data class InsightsUiState(val status: StatusUiState = StatusUiState(), val risks: RisksUiState = RisksUiState(), val trends: TrendsUiState = TrendsUiState(), val isLoading: Boolean = true, val hasEnoughHistory: Boolean = false, val currency: String = "INR")`
    
    - `data class StatusUiState(val netPosition: Double = 0.0, val netCashFlow: Double = 0.0, val income: Double = 0.0, val expense: Double = 0.0, val savings: Double = 0.0, val lending: Double = 0.0, val borrowing: Double = 0.0, val monthLabel: String = "", val hasTransactions: Boolean = false)`
    - Formular per STA-01: Net Position = income − expense − savings + borrowing − lending
    - Net Cash Flow = income − expense (STA-02)
    
    - `data class RiskAlert(val type: String, val severity: String, val icon: String, val title: String, val explanation: String, val triggeringValue: Double = 0.0)` — per RSK-07: icon + title + explanation with triggering number
    - `data class RisksUiState(val alerts: List<RiskAlert> = emptyList(), val hasTransactions: Boolean = false)` — max 3 alerts per RSK-01
    
    - `data class TrendsUiState(val dominantType: String = "", val dominantAmount: Double = 0.0, val dailyIncome: List&lt;Pair&lt;Int, Double&gt;&gt; = emptyList(), val dailyExpense: List&lt;Pair&lt;Int, Double&gt;&gt; = emptyList(), val hasTransactions: Boolean = false)` — per TRD-01 and TRD-02
    
    Place file in `app/src/main/java/com/moneymanager/app/ui/screens/`.
  </action>
  <verify>
    <automated>Get-Content "D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\app\ui\screens\InsightsUiState.kt" | Select-String "data class" | Measure-Object | Where-Object {$_.Count -ge 4}</automated>
  </verify>
  <done>InsightsUiState.kt contains 4+ data classes: InsightsUiState, StatusUiState, RisksUiState, TrendsUiState</done>
</task>

<task type="auto">
  <name>Task 2: Create InsightsCalculator pure Kotlin object</name>
  <files>app/src/main/java/com/moneymanager/app/ui/screens/InsightsCalculator.kt</files>
  <action>
    Create a pure Kotlin object (no Android imports) that computes all insights from transaction lists.
    
    Define:
    ```
    object InsightsCalculator {
        data class InsightsData(
            val status: StatusUiState,
            val risks: RisksUiState,
            val trends: TrendsUiState,
            val hasEnoughHistory: Boolean
        )
        
        fun compute(
            currentMonthTxs: List&lt;TransactionEntity&gt;,
            previousMonthTxs: List&lt;TransactionEntity&gt;
            calendar: Calendar = Calendar.getInstance()
        ): InsightsData {
            // Filter out split children FIRST per success criterion 2
            val current = currentMonthTxs.filter { !it.isSplitChild }
            val previous = previousMonthTxs.filter { !it.isSplitChild }
            
            // Compute all 7 figures from same window per success criterion 3
            // Income: type == "income"
            // Expense: type == "expense"
            // Savings: type == "savings" ONLY (not "investment" per Q1 resolution in context)
            // Lending: type == "lend"
            // Borrowing: type == "borrow"
            
            // Net Position = income − expense − savings + borrowing − lending (per Q2 resolution)
            // Net Cash Flow = income − expense
            
            // hasEnoughHistory = previousMonthTxs.isNotEmpty() per STATE.md
            
            // Risk rules (max 3, WARNING before INFO per RSK-01):
            // RSK-04: netPosition < 0 → WARNING
            // RSK-02: expense > income → WARNING  
            // RSK-05: borrowing > income * 0.5 → WARNING
            // RSK-03: expense increased >20% vs prev month → INFO (suppress if !hasEnoughHistory)
            // RSK-06: savings improved vs prev month → INFO (suppress if !hasEnoughHistory)
            
            // Trends:
            // TRD-01: Find type with highest total amount
            // TRD-02: Group by calendar day, sum income and expense separately
        }
    }
    ```
    
    Key rules from context/requirements:
    - No Android runtime imports (pure Kotlin only)
    - SAVINGS type only (Q1 resolved: no investment)
    - Net Position formula: income − expense − savings + borrowing − lending (Q2 resolved)
    - Separate rules for overspending and negative position (Q5 resolved)
    - hasEnoughHistory=false suppresses RSK-03 and RSK-06
    
    Place file in `app/src/main/java/com/moneymanager/app/ui/screens/`.
  </action>
  <verify>
    <automated>Get-Content "D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\app\ui\screens\InsightsCalculator.kt" | Select-String "object InsightsCalculator"</automated>
  </verify>
  <done>InsightsCalculator.kt created with compute() function that accepts two List&lt;TransactionEntity&gt; and returns InsightsData, no Android imports</done>
</task>

</tasks>

<verification>
- [ ] InsightsCalculator.compute() uses only Kotlin stdlib (no Android imports)
- [ ] All TransactionEntity type checks use: "income", "expense", "savings" (not "investment")
- [ ] Split children filtered: `filter { !it.isSplitChild }` applied to all aggregations
- [ ] Net Position formula matches: income − expense − savings + borrowing − lending
- [ ] Risk rules return max 3 alerts with WARNING severity before INFO
- [ ] hasEnoughHistory flag controls comparison-based rules (RSK-03, RSK-06)
</verification>

<success_criteria>
1. InsightsCalculator.compute() works with no Android runtime — pure Kotlin object
2. Split child transactions (isSplitChild=true) excluded from every aggregation
3. All 7 monetary figures (netPosition, netCashFlow, income, expense, savings, lending, borrowing) computed from same current-month date window
4. hasEnoughHistory flag suppresses comparison-based risk rules when no prev-month data
</success_criteria>

<output>
After completion, create `.planning/phases/27-data-layer-calculations/27-01-SUMMARY.md`
</output>
