---
phase: 36-dialog-integration-and-fab
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt
autonomous: true
requirements:
  - DRAFT-01
  - STD-04

must_haves:
  truths:
    - "Existing + FAB in TransactionsScreen opens AddEditTransactionDialog inline, behavior unchanged"
    - "When isAiAssistAvailable=true, a secondary AI Draft FAB appears above the existing + FAB"
    - "Tapping the AI Draft FAB expands to show SMS, Receipt, and Voice mini-FABs using AnimatedVisibility"
    - "Voice mini-FAB is hidden when SpeechRecognizer.isRecognitionAvailable(context) returns false"
    - "When isAiAssistAvailable=false, the AI Draft FAB group is absent entirely"
    - "Tapping a source mini-FAB calls the corresponding onNavigateToAiDraft* lambda"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt"
      provides: "Expandable AI Draft FAB group alongside existing + FAB in Scaffold.floatingActionButton"
      contains: "isAiAssistAvailable: Boolean = false"
  key_links:
    - from: "TransactionsScreen.kt floatingActionButton"
      to: "onNavigateToAiDraftSms / onNavigateToAiDraftReceipt / onNavigateToAiDraftVoice lambdas"
      via: "mini-FAB onClick handlers"
      pattern: "onNavigateToAiDraft"
---

<objective>
Add the expandable three-source AI Draft FAB group to TransactionsScreen alongside the existing + FAB.

Purpose: Users with an AICore-capable device see a secondary AI Draft FAB above the existing + FAB. Tapping it expands a small menu of SMS, Receipt, and Voice mini-FABs that navigate to the Phase 35 source screens. Users without AICore see no change — the AI FAB group is gated behind `isAiAssistAvailable`. The existing + FAB is untouched in behavior and position.

Output: TransactionsScreen.kt gains four new parameters (isAiAssistAvailable, three nav lambdas) and a FAB group composable in the floatingActionButton slot. The existing FAB onClick is unchanged.
</objective>

<execution_context>
@D:\Android projec\.planning\..\..\.claude\get-shit-done\workflows\execute-plan.md
@D:\Android projec\.planning\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\Android projec\.planning\ROADMAP.md
@D:\Android projec\.planning\STATE.md
@D:\Android projec\.planning\phases\36-dialog-integration-and-fab\36-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From TransactionsScreen.kt — current function signature:
```kotlin
fun TransactionsScreen(
    viewModel: TransactionsViewModel,
    initialType: String? = null,
    initialAccountId: Long? = null,
    initialStartDate: Long? = null,
    initialEndDate: Long? = null,
    initialGoalId: Long? = null,
    initialCategoryId: Long? = null,
    initialPeerId: Long? = null
)
```

From TransactionsScreen.kt — current FAB (line ~443):
```kotlin
floatingActionButton = {
    FloatingActionButton(
        onClick = { showAddDialog = true },
        containerColor = MaterialTheme.colorScheme.primaryContainer,
        contentColor = MaterialTheme.colorScheme.onPrimaryContainer,
        shape = CircleShape
    ) {
        Icon(Icons.Default.Add, contentDescription = "Add Transaction")
    }
}
```

From MoneyManagerNavHost.kt (Plan 36-01 output) — caller passes:
```kotlin
TransactionsScreen(
    viewModel = hiltViewModel(),
    isAiAssistAvailable = isAiAvailable,
    onNavigateToAiDraftSms = { navController.navigate(Screen.AiDraftSms.route) },
    onNavigateToAiDraftReceipt = { navController.navigate(Screen.AiDraftReceipt.route) },
    onNavigateToAiDraftVoice = { navController.navigate(Screen.AiDraftVoice.route) },
    initialType = type,
    ...
)
```

SpeechRecognizer availability check pattern (Android platform API):
```kotlin
import android.speech.SpeechRecognizer
val context = LocalContext.current
val isSpeechAvailable = SpeechRecognizer.isRecognitionAvailable(context)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add four new parameters to TransactionsScreen and implement expandable AI Draft FAB</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/transactions/TransactionsScreen.kt
  </read_first>
  <action>
    STEP 1 — Add four new parameters at the end of the TransactionsScreen signature, each with null/false defaults so MoneyManagerNavHost callers that don't pass them still compile:
    - `isAiAssistAvailable: Boolean = false`
    - `onNavigateToAiDraftSms: (() -> Unit)? = null`
    - `onNavigateToAiDraftReceipt: (() -> Unit)? = null`
    - `onNavigateToAiDraftVoice: (() -> Unit)? = null`

    STEP 2 — Add a local state variable in the composable body (near the other `var` declarations at the top of the function):
    - `var aiDraftExpanded by remember { mutableStateOf(false) }`

    STEP 3 — Replace the current `floatingActionButton = { ... }` lambda content with a Column layout that stacks the FABs vertically. The Column uses `Arrangement.spacedBy(8.dp)` and `horizontalAlignment = Alignment.End`. Full structure:
    
    a) If `isAiAssistAvailable` is true, render an `AnimatedVisibility(visible = aiDraftExpanded, enter = fadeIn() + expandVertically(expandFrom = Alignment.Bottom), exit = fadeOut() + shrinkVertically(shrinkTowards = Alignment.Bottom))` block containing a Column of mini-FABs:
       - SMS mini-FAB: `SmallFloatingActionButton(onClick = { onNavigateToAiDraftSms?.invoke(); aiDraftExpanded = false }, containerColor = MaterialTheme.colorScheme.secondaryContainer, contentColor = MaterialTheme.colorScheme.onSecondaryContainer)` with Icon `Icons.Default.Sms` and contentDescription "AI Draft from SMS".
       - Receipt mini-FAB: `SmallFloatingActionButton(onClick = { onNavigateToAiDraftReceipt?.invoke(); aiDraftExpanded = false }, containerColor = MaterialTheme.colorScheme.secondaryContainer, contentColor = MaterialTheme.colorScheme.onSecondaryContainer)` with Icon `Icons.Default.Receipt` and contentDescription "AI Draft from Receipt".
       - Voice mini-FAB: only rendered when `SpeechRecognizer.isRecognitionAvailable(LocalContext.current)` is true. Same SmallFloatingActionButton pattern with Icon `Icons.Default.Mic` and contentDescription "AI Draft from Voice" and `onClick = { onNavigateToAiDraftVoice?.invoke(); aiDraftExpanded = false }`.
    
    b) If `isAiAssistAvailable` is true, render the AI Draft toggle FAB directly below the AnimatedVisibility block (but above the + FAB):
       `FloatingActionButton(onClick = { aiDraftExpanded = !aiDraftExpanded }, containerColor = MaterialTheme.colorScheme.tertiaryContainer, contentColor = MaterialTheme.colorScheme.onTertiaryContainer, shape = CircleShape)` with Icon `Icons.Default.AutoAwesome` and contentDescription "AI Draft Options".
    
    c) Always render the existing + FAB at the bottom of the Column (exact code preserved, only wrapped in Column):
    `FloatingActionButton(onClick = { showAddDialog = true }, containerColor = MaterialTheme.colorScheme.primaryContainer, contentColor = MaterialTheme.colorScheme.onPrimaryContainer, shape = CircleShape)` with Icon `Icons.Default.Add` and contentDescription "Add Transaction".

    STEP 4 — Dismiss expanded state when user navigates away or opens the inline dialog. Add `LaunchedEffect(showAddDialog) { if (showAddDialog) aiDraftExpanded = false }`.

    STEP 5 — Add required imports not already present:
    - `android.speech.SpeechRecognizer`
    - `androidx.compose.material3.SmallFloatingActionButton`
    - `androidx.compose.material.icons.filled.AutoAwesome`
    - `androidx.compose.material.icons.filled.Sms`
    - `androidx.compose.material.icons.filled.Receipt`
    - `androidx.compose.material.icons.filled.Mic`
    - `androidx.compose.foundation.layout.Column` (already likely present)
    - `androidx.compose.animation.expandVertically` (already present)
    - `androidx.compose.animation.shrinkVertically` (already present)
    - `androidx.compose.animation.fadeIn` (already present)
    - `androidx.compose.animation.fadeOut` (already present)
    - `androidx.compose.ui.Alignment` (already present)
  </action>
  <verify>
    <automated>cd "D:\Android projec\MoneyManager" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TransactionsScreen.kt function signature contains `isAiAssistAvailable: Boolean = false`
    - TransactionsScreen.kt contains `onNavigateToAiDraftSms: (() -> Unit)? = null`
    - TransactionsScreen.kt contains `onNavigateToAiDraftReceipt: (() -> Unit)? = null`
    - TransactionsScreen.kt contains `onNavigateToAiDraftVoice: (() -> Unit)? = null`
    - TransactionsScreen.kt contains `var aiDraftExpanded by remember { mutableStateOf(false) }`
    - TransactionsScreen.kt contains `SpeechRecognizer.isRecognitionAvailable(` guard for voice mini-FAB
    - TransactionsScreen.kt contains `SmallFloatingActionButton` at least three times (SMS, Receipt, Voice)
    - TransactionsScreen.kt contains `Icons.Default.AutoAwesome` for the toggle FAB
    - TransactionsScreen.kt still contains `onClick = { showAddDialog = true }` in the existing + FAB (behavior unchanged)
    - TransactionsScreen.kt contains `if (isAiAssistAvailable)` guard wrapping the AI FAB group
    - Kotlin compilation passes with no errors
  </acceptance_criteria>
  <done>TransactionsScreen renders an expandable AI Draft FAB group when isAiAssistAvailable=true; Voice mini-FAB is conditionally hidden; existing + FAB behavior is fully preserved; compilation succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Expandable AI Draft FAB group in TransactionsScreen. The UI is controlled by the isAiAssistAvailable boolean which comes from AiAvailabilityRepository. On a real AICore device the group would show; on an emulator or non-AICore device isAiAvailable=false so the group is absent.</what-built>
  <how-to-verify>
    1. Build and run the app: `./gradlew :app:installDebug` then launch on emulator or device
    2. Navigate to Transactions screen (center bottom nav item)
    3. On a non-AICore emulator: confirm only the existing + FAB (blue circle) is visible — no AI FAB. This is the PASS condition for non-AI devices.
    4. To force-test the AI FAB group without real AICore: temporarily set `isAiAssistAvailable = true` hardcoded in MoneyManagerNavHost.kt TransactionsScreen call, rerun, verify: an AutoAwesome FAB appears above the + FAB; tapping it reveals SMS, Receipt, and Voice mini-FABs using AnimatedVisibility; tapping + FAB still opens the inline add dialog.
    5. Revert the hardcoded override after verification.
  </how-to-verify>
  <resume-signal>Type "approved" once the existing + FAB behavior is confirmed unchanged and the AI FAB group expands/collapses correctly (even if tested via hardcoded flag). Describe any visual issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SpeechRecognizer.isRecognitionAvailable() | Platform API call — always safe, no user data |
| onNavigateToAiDraftSms/Receipt/Voice lambdas | Nav callbacks injected by NavHost — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Spoofing | isAiAssistAvailable flag | accept | Flag derived from AiAvailabilityRepository backed by DataStore "READY"/"NEVER"/"PENDING" string — no external network control |
| T-36-05 | Elevation of Privilege | Voice FAB shown on device without speech support | mitigate | Voice mini-FAB is only rendered when `SpeechRecognizer.isRecognitionAvailable(context) == true` |
</threat_model>

<verification>
1. Run `./gradlew :app:compileDebugKotlin` — must succeed with 0 errors
2. Grep `isAiAssistAvailable` in TransactionsScreen.kt — must appear in signature and guard
3. Grep `SmallFloatingActionButton` in TransactionsScreen.kt — must appear at least 3 times
4. Grep `showAddDialog = true` in TransactionsScreen.kt — must still exist in existing + FAB onClick (behavior preserved)
5. Grep `SpeechRecognizer.isRecognitionAvailable` in TransactionsScreen.kt — must appear once in voice mini-FAB guard
</verification>

<success_criteria>
- TransactionsScreen has four new null-default parameters (isAiAssistAvailable, three nav lambdas)
- Expandable AI Draft FAB group visible only when isAiAssistAvailable=true
- Voice mini-FAB conditionally hidden when SpeechRecognizer.isRecognitionAvailable=false
- Existing + FAB onClick behavior unchanged
- Human verification confirms visual correctness
- Kotlin compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/36-dialog-integration-and-fab/36-02-SUMMARY.md` following the template at `@D:\Android projec\.planning\..\..\.claude\get-shit-done\templates\summary.md`.
</output>
