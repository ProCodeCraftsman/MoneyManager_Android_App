---
phase: 36-dialog-integration-and-fab
plan: 04
type: execute
wave: 3
depends_on:
  - 36-03
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt
autonomous: false
requirements:
  - DRAFT-04

must_haves:
  truths:
    - "Fields populated from initialDraft have a tinted background (8% primary) and a sparkle badge (Icons.Default.AutoAwesome)"
    - "Editing a field that has AI highlighting removes only that field's tint and badge"
    - "Fields not populated by the draft have no tint or badge"
    - "Manual entry fields (transaction=null, initialDraft=null) have no tint or badge at all"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt"
      provides: "aiSuggestedFields state set; per-field tinted box + sparkle badge wrapping"
      contains: "var aiSuggestedFields by remember"
  key_links:
    - from: "LaunchedEffect(initialDraft) in AddEditTransactionDialog"
      to: "aiSuggestedFields mutableStateOf set"
      via: "populating aiSuggestedFields with names of non-null draft fields after applying them"
      pattern: "aiSuggestedFields.*add\\|buildSet"
    - from: "each AI-highlighted field edit handler"
      to: "aiSuggestedFields"
      via: "aiSuggestedFields = aiSuggestedFields - \"fieldName\""
      pattern: "aiSuggestedFields.*minus\\|aiSuggestedFields - "
---

<objective>
Add AI field highlighting: fields populated from initialDraft are visually distinguished with a tinted container and sparkle badge; editing any highlighted field clears its highlight.

Purpose: Users can instantly see which form fields were pre-filled by AI vs. which they entered manually. Editing a field signals explicit user ownership of that value. The highlighting uses the existing Material 3 color scheme tokens and requires no new dependencies.

Output: AddEditTransactionDialog.kt gains an `aiSuggestedFields` state set. Task 1 declares the state and populates it in LaunchedEffect. Task 2a wraps the type/amount/date/category fields. Task 2b wraps the account/peer/tags/note fields. Human verification confirms the visual result.
</objective>

<execution_context>
@D:\Android projec\.planning\..\..\.claude\get-shit-done\workflows\execute-plan.md
@D:\Android projec\.planning\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\Android projec\.planning\ROADMAP.md
@D:\Android projec\.planning\STATE.md
@D:\Android projec\.planning\phases\36-dialog-integration-and-fab\36-03-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase after Plan 36-03 runs. -->

From AddEditTransactionDialog.kt (after Plan 36-03 modifications):
- `initialDraft: TransactionDraft? = null` parameter is in signature
- `LaunchedEffect(initialDraft)` block applies draft fields to: `type`, `amount`, `selectedCategoryId`, `selectedAccountId`, `selectedPeerId`, `selectedTagIds`, `description`, `selectedDate`
- The draft fields to track by name (used as string keys in aiSuggestedFields):
  - `"type"` — when `initialDraft.typeId != null`
  - `"amount"` — when `initialDraft.amount != null`
  - `"category"` — when `initialDraft.categoryId != null`
  - `"account"` — when `initialDraft.accountId != null`
  - `"peer"` — when `initialDraft.peerContactId != null`
  - `"tags"` — when `initialDraft.tagIds.isNotEmpty()`
  - `"note"` — when `initialDraft.description != null || initialDraft.note != null`
  - `"date"` — when `initialDraft.date != null`

Material 3 Compose tinting pattern:
```kotlin
// Tinted container wrapper:
Box(
    modifier = Modifier
        .background(
            MaterialTheme.colorScheme.primary.copy(alpha = 0.08f),
            RoundedCornerShape(4.dp)
        )
        .padding(2.dp)
) { /* field content */ }

// Sparkle badge pattern (BadgedBox from Material 3):
BadgedBox(
    badge = {
        if ("fieldName" in aiSuggestedFields) {
            Badge(containerColor = MaterialTheme.colorScheme.primary) {
                Icon(
                    Icons.Default.AutoAwesome,
                    contentDescription = "AI suggested",
                    modifier = Modifier.size(10.dp),
                    tint = MaterialTheme.colorScheme.onPrimary
                )
            }
        }
    }
) { /* tinted Box with field */ }
```

Field edit-handler pattern — each field has an existing onChange / onClick:
- `type` selector: existing `onClick` lambda when user taps Income/Expense/etc.
- `amount` field: existing onChange or text field onValueChange
- `category` selector: existing onClick / onCategorySelected lambda
- `account` dropdown: existing selectedAccountId assignment
- `peer` selector: existing selectedPeerId assignment
- `tags` chip area: existing selectedTagIds modification
- `note` text field: existing `description` text field onValueChange
- `date` picker: existing `selectedDate` assignment
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add aiSuggestedFields state and populate from LaunchedEffect</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt
  </read_first>
  <action>
    STEP 1 — Add a new state variable in the composable body, immediately after the `isEdit` declaration and before the `rememberSaveable` state vars:
    `var aiSuggestedFields by remember { mutableStateOf(emptySet<String>()) }`

    Use `remember` (not `rememberSaveable`) since the AI suggestion set does not need to survive configuration changes — on recomposition after a config change the draft is re-applied by LaunchedEffect anyway.

    STEP 2 — Extend the existing `LaunchedEffect(initialDraft)` block added in Plan 36-03. After all the state var assignments, add population of `aiSuggestedFields`. Build the set inline using `buildSet { }`:

    ```
    aiSuggestedFields = buildSet {
        if (initialDraft.typeId != null) add("type")
        if (initialDraft.amount != null) add("amount")
        if (initialDraft.categoryId != null) add("category")
        if (initialDraft.accountId != null) add("account")
        if (initialDraft.peerContactId != null) add("peer")
        if (initialDraft.tagIds.isNotEmpty()) add("tags")
        if (initialDraft.description != null || initialDraft.note != null) add("note")
        if (initialDraft.date != null) add("date")
    }
    ```

    The full `if (initialDraft != null)` guard from Plan 36-03 surrounds both the state var assignments and this `aiSuggestedFields` population.

    STEP 3 — In the `onDismiss`-triggered code path (the combined lambda from Plan 36-03 that calls `onDraftDismiss?.invoke()` then `onDismiss()`), also reset: `aiSuggestedFields = emptySet()` before calling `onDraftDismiss?.invoke()`. This ensures a clean state if the dialog is reused.
  </action>
  <verify>
    <automated>cd "D:\Android projec\MoneyManager" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AddEditTransactionDialog.kt contains `var aiSuggestedFields by remember { mutableStateOf(emptySet<String>()) }`
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = buildSet {` inside the LaunchedEffect block
    - AddEditTransactionDialog.kt contains `add("type")`, `add("amount")`, `add("category")` inside the buildSet block
    - AddEditTransactionDialog.kt contains `add("note")` inside the buildSet block
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = emptySet()` in the dismiss path
    - Kotlin compilation passes with no errors
  </acceptance_criteria>
  <done>aiSuggestedFields is declared and populated in LaunchedEffect; reset on dismiss; ready for field-level highlighting in Tasks 2a and 2b.</done>
</task>

<task type="auto">
  <name>Task 2a: Wrap type, amount, date, and category fields with tinted Box and sparkle badge</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt
  </read_first>
  <action>
    Apply the tint/badge/edit-clear pattern to four fields. For each field:

    TINT WRAPPER — Wrap the field's existing composable in a conditional Box:
    ```
    val isAiField = "fieldName" in aiSuggestedFields
    Box(
        modifier = if (isAiField) Modifier.background(
            MaterialTheme.colorScheme.primary.copy(alpha = 0.08f),
            RoundedCornerShape(4.dp)
        ).padding(horizontal = 2.dp) else Modifier
    ) { /* existing field composable */ }
    ```

    BADGE WRAPPER — Additionally wrap with BadgedBox:
    ```
    BadgedBox(
        badge = {
            if (isAiField) {
                Badge(containerColor = MaterialTheme.colorScheme.primary) {
                    Icon(Icons.Default.AutoAwesome, contentDescription = "AI suggested",
                         modifier = Modifier.size(10.dp), tint = MaterialTheme.colorScheme.onPrimary)
                }
            }
        }
    ) { /* tinted Box */ }
    ```

    EDIT CLEAR — In each field's existing edit handler (onValueChange, onClick, etc.), prepend:
    `aiSuggestedFields = aiSuggestedFields - "fieldName"`
    Do not replace existing logic — prepend the clear.

    Apply to these four fields:

    1. TYPE selector (the Income/Expense/Savings/etc. chip row or tab row):
       - Field key: `"type"`
       - Wrap the entire type selector composable.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "type"` in the existing type change lambda.

    2. AMOUNT field (the amount display text or NumericKeypad input):
       - Field key: `"amount"`
       - Wrap the amount display Row or Box that shows the amount value.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "amount"` in the existing amount change or keypad onKey lambda.

    3. DATE display (the date row that opens the date picker):
       - Field key: `"date"`
       - Wrap the date display Row.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "date"` when `selectedDate` is changed from the date picker callback.

    4. CATEGORY selector (the category row/button that sets selectedCategoryId):
       - Field key: `"category"`
       - Wrap the category selector Row.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "category"` when selectedCategoryId is changed.

    IMPORTANT implementation rules:
    - The tint Box and BadgedBox are purely additive wrappers — do NOT restructure or reorder existing layout. Wrap existing composables in-place.
    - If a field is already inside a Row or Column, wrap that Row/Column — do not split inner elements.
    - `Icons.Default.AutoAwesome` is from `androidx.compose.material.icons.filled`. If `AutoAwesome` is not available in the project's material-icons-extended version, use `Icons.Default.Stars` as fallback. Check existing imports for available icons before choosing.
    - `BadgedBox` is from `androidx.compose.material3` (already imported as `material3.*`).
    - `RoundedCornerShape` is from `androidx.compose.foundation.shape` — already imported.
  </action>
  <verify>
    <automated>cd "D:\Android projec\MoneyManager" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AddEditTransactionDialog.kt contains `"type" in aiSuggestedFields` (or equivalent `isAiField` pattern for type field)
    - AddEditTransactionDialog.kt contains `"amount" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `"date" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `"category" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `primary.copy(alpha = 0.08f)` — the tint alpha value (at least 1 instance)
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = aiSuggestedFields - "type"` (or equivalent minus pattern)
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = aiSuggestedFields - "amount"` pattern
    - AddEditTransactionDialog.kt contains `BadgedBox` at least 2 times (one per badge-wrapped field in this task)
    - Kotlin compilation passes with no errors
  </acceptance_criteria>
  <done>Type, amount, date, and category fields have tinted backgrounds and sparkle badges when AI-suggested; each field's edit handler clears only its own highlight; compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2b: Wrap account, peer, tags, and note fields with tinted Box and sparkle badge</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/app/ui/dialogs/AddEditTransactionDialog.kt
  </read_first>
  <action>
    Apply the same tint/badge/edit-clear pattern (from Task 2a) to the remaining four fields.

    TINT WRAPPER, BADGE WRAPPER, and EDIT CLEAR follow the identical pattern described in Task 2a. Apply to:

    5. ACCOUNT dropdown (the account selector that sets selectedAccountId):
       - Field key: `"account"`
       - Wrap the account selector Row.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "account"` when selectedAccountId is changed.

    6. PEER selector (the peer contact row that sets selectedPeerId):
       - Field key: `"peer"`
       - Wrap the peer Row.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "peer"` when selectedPeerId is changed.

    7. TAGS area (the tag chips row or input):
       - Field key: `"tags"`
       - Wrap the tag section Row or Column.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "tags"` when selectedTagIds is modified (any add or remove).

    8. NOTE field (the description/note TextField):
       - Field key: `"note"`
       - Wrap the note TextField.
       - Prepend `aiSuggestedFields = aiSuggestedFields - "note"` in the onValueChange lambda of the description TextField.

    IMPORTANT implementation rules (same as Task 2a):
    - Wrappers are purely additive — do NOT restructure or reorder existing layout.
    - Wrap the entire existing composable in-place.
    - `BadgedBox` is from `androidx.compose.material3`.
    - `Icons.Default.AutoAwesome` or fallback `Icons.Default.Stars` — use whichever was used in Task 2a for consistency.
  </action>
  <verify>
    <automated>cd "D:\Android projec\MoneyManager" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AddEditTransactionDialog.kt contains `"account" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `"peer" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `"tags" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `"note" in aiSuggestedFields` pattern
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = aiSuggestedFields - "note"` (or equivalent minus pattern)
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = aiSuggestedFields - "account"` pattern
    - AddEditTransactionDialog.kt contains `BadgedBox` at least 6 times total across Tasks 2a and 2b (8 fields, some may share a BadgedBox wrapper from 2a)
    - AddEditTransactionDialog.kt contains `aiSuggestedFields = aiSuggestedFields - "` at least 6 distinct field names total (combined with Task 2a)
    - Kotlin compilation passes with no errors
  </acceptance_criteria>
  <done>All 8 draft-populated fields have tinted backgrounds and sparkle badges when AI-suggested; each field's edit handler clears only its own highlight; non-AI fields are never highlighted; compilation succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AI field highlighting in AddEditTransactionDialog. Tinted container (8% primary) + sparkle badge on AI-populated fields; editing any field removes its highlight.</what-built>
  <how-to-verify>
    1. Build and install: `./gradlew :app:installDebug`
    2. Navigate to Transactions screen. Use the existing + FAB to open Add Transaction manually — confirm NO tinted fields, NO sparkle badges. This is baseline.
    3. To test AI field highlighting without real AICore, temporarily navigate directly to AddTransactionScreen via adb deep link with a mock draftJson. Build a URL-encoded JSON: `adb shell am start -d "moneymanager://add_transaction?type=expense" com.moneymanager.app` then verify no highlighting (since no draftJson).
    4. Alternatively, in AddTransactionScreen.kt, temporarily pass a hardcoded `initialDraft = TransactionDraft(typeId = "expense", amount = 1500.0, description = "Test AI draft", sourceType = "sms", sourceSender = "HDFCBK")` to verify: source banner shows at top of dialog; amount field has tinted background; editing the amount field removes its tint immediately.
    5. Revert the hardcoded draft after verification.
    6. Confirm: editing the amount field removes only the amount tint; the category field (if highlighted) retains its tint until edited; manually-entered fields never show tint.
  </how-to-verify>
  <resume-signal>Type "approved" once field-level highlighting and edit-clear behavior is confirmed working. Describe any visual or behavioral issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aiSuggestedFields set | Internal UI state derived from TransactionDraft; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Spoofing | aiSuggestedFields visual indicator misleads user | accept | The badge is purely cosmetic and advisory; user retains full edit control; the field value is theirs to change or keep |
| T-36-10 | Denial of Service | remember vs rememberSaveable for aiSuggestedFields | accept | Using `remember` (not rememberSaveable) means config change clears the set; LaunchedEffect re-populates from initialDraft on recomposition, so state is consistent |
</threat_model>

<verification>
1. Run `./gradlew :app:compileDebugKotlin` — must succeed with 0 errors
2. Grep `aiSuggestedFields` in AddEditTransactionDialog.kt — must return 10+ lines (declaration + buildSet + multiple field guards + edit clears)
3. Grep `primary.copy(alpha = 0.08f)` in AddEditTransactionDialog.kt — must return 1+ matches
4. Grep `BadgedBox` in AddEditTransactionDialog.kt — must return 3+ matches
5. Grep `aiSuggestedFields - "` in AddEditTransactionDialog.kt — must return 6+ matches (edit clear handlers for all 8 fields, 2 covered per Task 2a check)
</verification>

<success_criteria>
- aiSuggestedFields state set declared and populated in LaunchedEffect (DRAFT-04)
- 8 fields wrapped with conditional tint and badge — only when name is in aiSuggestedFields
- Each field's edit handler clears its own name from aiSuggestedFields (DRAFT-04)
- Manual entry dialog (initialDraft=null) has zero tinted fields — aiSuggestedFields stays empty
- Human verification confirms visual highlighting and per-field edit-clear behavior
- Kotlin compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/36-dialog-integration-and-fab/36-04-SUMMARY.md` following the template at `@D:\Android projec\.planning\..\..\.claude\get-shit-done\templates\summary.md`.
</output>
