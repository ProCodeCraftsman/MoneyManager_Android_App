---
phase: 31-trends-pane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneHeader.kt
  - app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsDominanceCard.kt
  - app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsLineChart.kt
  - app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneEmptyState.kt
autonomous: true
requirements: [TRD-01, TRD-02, TRD-03, TRD-04]
user_setup: []
must_haves:
  truths:
    - TrendsPaneHeader displays "Trends" heading (headlineMedium)
    - TrendsDominanceCard shows highest-total transaction type with amount and currency
    - TrendsLineChart renders dual-series line chart (income green, expense red) by calendar day
    - TrendsLineChart hidden when <2 data points (returns empty composable)
    - TrendsPaneEmptyState displays "No transactions this month" when no data
  artifacts:
    - path: "app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneHeader.kt"
      provides: "Pane header with Trends title"
      contains: "fun TrendsPaneHeader"
    - path: "app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsDominanceCard.kt"
      provides: "Dominant activity card showing top transaction type + amount"
      contains: "fun TrendsDominanceCard"
    - path: "app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsLineChart.kt"
      provides: "Dual-series line chart for daily income/expense"
      contains: "fun TrendsLineChart"
    - path: "app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneEmptyState.kt"
      provides: "Empty state view for no transactions"
      contains: "fun TrendsPaneEmptyState"
  key_links:
    - from: "TrendsDominanceCard.kt"
      to: "TrendsUiState"
      via: "accepts dominantType: String, dominantAmount: Double parameters"
    - from: "TrendsLineChart.kt"
      to: "TrendsUiState"
      via: "accepts dailyIncome: List<Pair<Int, Double>>, dailyExpense: List<Pair<Int, Double>> parameters"
---

<objective>
Create stateless Jetpack Compose composables for the Trends Pane UI components per TRD-01 to TRD-04.
Purpose: Implement the visual building blocks for displaying dominant activity and daily trend chart, following Material 3 design and Phase 29/30 patterns.
Output: 4 stateless composable files ready for integration into TrendsPaneScreen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-status-pane/29-01-PLAN.md
@.planning/phases/30-risks-pane/030-01-PLAN.md

<interfaces>
From InsightsUiState.kt:
```kotlin
data class TrendsUiState(
    val dominantType: String = "",
    val dominantAmount: Double = 0.0,
    val dailyIncome: List<Pair<Int, Double>> = emptyList(),  // (dayOfMonth, amount)
    val dailyExpense: List<Pair<Int, Double>> = emptyList(), // (dayOfMonth, amount)
    val hasTransactions: Boolean = false
)
```

From InsightsCalculator.kt (already implemented):
- TRD-01: Dominant type computed via `typeTotals.maxByOrNull { it.value }`
- TRD-02: Daily income/expense grouped by calendar day via `groupBy { dateFormat.format(it.date).toInt() }`
- TRD-03: Chart visibility controlled by `dailyIncome.size >= 2 || dailyExpense.size >= 2`

Note: Using Canvas-based line chart (no external chart library dependency needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create TrendsPaneHeader and TrendsPaneEmptyState composables</name>
  <files>app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneHeader.kt, app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneEmptyState.kt</files>
  <action>Create 2 stateless Jetpack Compose composables:

  1. **TrendsPaneHeader**: No parameters. Renders "Trends" text using Material 3 headlineMedium typography (28sp, Semibold 600 weight). Text color = MaterialTheme.colorScheme.primary. Matches RisksPaneHeader pattern.

  2. **TrendsPaneEmptyState**: No parameters. Renders full-screen centered Column with:
      - Text: "No transactions this month" (per TRD-04)
      - Typography: bodyLarge (16sp, Regular 400)
      - Text color: MaterialTheme.colorScheme.onSurfaceVariant
      - Fill entire screen (fillMaxSize modifier)
      - Centered vertically and horizontally (wrap in Box with Alignment.Center)
  
  Use Material 3 components (Text, Column, Box) only. No internal state. Follow Phase 29/30 empty state patterns.
  </action>
  <verify>
    <automated>ls app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneHeader.kt app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsPaneEmptyState.kt 2>&1 | wc -l</automated>
  </verify>
  <done>Both composable files exist, TrendsPaneHeader shows "Trends" with headlineMedium, TrendsPaneEmptyState shows exact TRD-04 copy "No transactions this month".</done>
</task>

<task type="auto">
  <name>task 2: create TrendsDominanceCard composable</name>
  <files>app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsDominanceCard.kt</files>
  <action>Create stateless TrendsDominanceCard composable:

  **TrendsDominanceCard**: Accepts `dominantType: String`, `dominantAmount: Double`, `currencySymbol: String` parameters. Renders:
      - Card with 16dp internal padding (md token)
      - Column layout (vertical)
      - Top: "Dominant Activity" label (bodyMedium, onSurfaceVariant)
      - Middle: Transaction type name (titleLarge, semibold, primary color) — capitalize first letter
      - Bottom: Amount with currency symbol (headlineSmall, semibold, primary color)
      - Card elevation: 1dp (default Card)
      - Card background: MaterialTheme.colorScheme.surfaceVariant
      - If dominantType is empty: renders empty box (called only when hasTransactions=true)
  
  Follow Phase 29 StatusPaneFigure pattern for typography and spacing.
  </action>
  <verify>
    <automated>ls app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsDominanceCard.kt 2>&1 | wc -l</automated>
  </verify>
  <done>TrendsDominanceCard.kt exists, accepts correct parameters, displays type + amount with currency, follows Phase 29 typography/spacing patterns.</done>
</task>

<task type="auto">
  <name>task 3: create TrendsLineChart composable (Canvas-based)</name>
  <files>app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsLineChart.kt</files>
  <action>Create stateless TrendsLineChart composable using Canvas (no external chart library):

  **TrendsLineChart**: Accepts `dailyIncome: List&lt;Pair&lt;Int, Double&gt;&gt;`, `dailyExpense: List&lt;Pair&lt;Int, Double&gt;&gt;`, `currencySymbol: String` parameters. Renders:
      - If `dailyIncome.size < 2 && dailyExpense.size < 2`: returns empty (TRD-03)
      - Canvas with height = 200.dp, fillMaxWidth
      - Merges both lists, finds min/max day (X-axis) and min/max amount (Y-axis)
      - Draws X-axis labels (day numbers) at bottom
      - Draws Y-axis labels (amounts with currency) at left
      - Plots income line (green = Color(0xFF4CAF50)) with dots at each data point
      - Plots expense line (red = Color(0xFFF44336)) with dots at each data point
      - Uses `drawLine` and `drawCircle` Canvas API
      - Chart background: subtle grid lines (onSurface at 10% opacity)
      - Chart title: "Daily Income vs Expense" (bodySmall, onSurfaceVariant, centered above chart)
  
  Note: Keep implementation simple but functional. No external dependencies. Pure Canvas drawing.
  </action>
  <verify>
    <automated>ls app/src/main/java/com/moneymanager/app/ui/insights/trends/TrendsLineChart.kt 2>&1 | wc -l</automated>
  </verify>
  <done>TrendsLineChart.kt exists, uses Canvas API, renders dual series, hidden when <2 data points, income=green line, expense=red line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local UI | All data from local ViewModel, no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-31-01 | I | All Composables | accept | Only render user's own financial data from local ViewModel |
| T-31-02 | D | All Composables | accept | No external dependencies, static UI components |
| T-31-03 | E | Currency/amount display | mitigate | Values come from InsightsCalculator (trusted source), displayed via Canvas/Text composables |
</threat_model>

<verification>
1. All composables compile without errors
2. TRD-01: Dominant activity card shows type + amount with currency symbol
3. TRD-02: Dual-series line chart renders income (green) and expense (red)
4. TRD-03: Chart hidden when <2 data points for both series
5. TRD-04: Empty state shows "No transactions this month"
6. Typography matches Phase 29/30 patterns (headlineMedium for header, titleLarge for card titles)
7. No external chart library dependency added
</verification>

<success_criteria>
4 stateless composable files created (TrendsPaneHeader, TrendsDominanceCard, TrendsLineChart, TrendsPaneEmptyState), matching TRD requirements, no internal state, all data via parameters, Canvas-based chart without external dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/31-trends-pane/31-01-SUMMARY.md`
</output>
