---
phase: 18.1-split-expand-fix
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
autonomous: false
requirements:
  - SPLIT-01
  - SPLIT-02
  - SPLIT-03
---

<objective>
Complete the split transaction expandable view implementation. Add toggle function, SplitTransactionCard composable, and wire up split detection in the transaction list.
</objective>

<context>
@.planning/phases/18-split-transaction-expand/18-CONTEXT.md
@MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toggleSplitExpand function</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <action>
Add the toggleSplitExpand function after the updatePeriodBasedOnFilter function (around line 185).

```kotlin
fun toggleSplitExpand(transactionId: Long) {
    expandedSplitIds = if (expandedSplitIds.contains(transactionId)) {
        expandedSplitIds - transactionId
    } else {
        expandedSplitIds + transactionId
    }
}
```
  </action>
  <verify>grep -n "fun toggleSplitExpand" TransactionsScreen.kt</verify>
  <done>toggleSplitExpand function exists</done>
</task>

<task type="auto">
  <name>Task 2: Create SplitTransactionCard composable</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <action>
Add the SplitTransactionCard composable before TransactionItem (around line 710).

See 18-01-PLAN.md for the full implementation.
  </action>
  <verify>grep -n "fun SplitTransactionCard" TransactionsScreen.kt</verify>
  <done>SplitTransactionCard composable exists</done>
</task>

<task type="auto">
  <name>Task 3: Wire up split detection in LazyColumn</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <action>
Modify the transaction list rendering (line 559) to detect isSplitParent and render SplitTransactionCard instead of TransactionItem.

```kotlin
items(transactions, key = { it.id }) { tx ->
    if (tx.isSplitParent) {
        val splitChildren by viewModel.getSplitChildren(tx.id).collectAsStateWithLifecycle(initialValue = emptyList())
        val isExpanded = expandedSplitIds.contains(tx.id)
        
        SplitTransactionCard(
            parentTransaction = tx,
            splitChildren = splitChildren,
            accounts = uiState.allAccounts,
            categories = uiState.allCategories,
            peers = uiState.allPeers,
            currencyFormat = currencyFormat,
            isExpanded = isExpanded,
            onToggleExpand = { toggleSplitExpand(tx.id) },
            onEdit = { editingTransaction = tx },
            onDelete = { viewModel.deleteTransaction(tx) }
        )
    } else {
        TransactionItem(
            transaction = tx,
            accounts = uiState.allAccounts,
            categories = uiState.allCategories,
            peers = uiState.allPeers,
            currencyFormat = currencyFormat,
            onEdit = { editingTransaction = tx },
            onDelete = { viewModel.deleteTransaction(tx) }
        )
    }
}
```
  </action>
  <verify>grep -n "isSplitParent.*SplitTransactionCard" TransactionsScreen.kt</verify>
  <done>Split detection wired up in transaction list</done>
</task>

</tasks>

<verification>
1. Build compiles: ./gradlew.bat compileDebugKotlin
2. Split transactions show with expand icon
3. Tap expand reveals child transactions
4. Tap collapse hides them
</verification>

<success_criteria>
- [ ] toggleSplitExpand function added
- [ ] SplitTransactionCard composable added
- [ ] LazyColumn wired with split detection
- [ ] Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-split-expand-fix/18.1-01-SUMMARY.md`
</output>