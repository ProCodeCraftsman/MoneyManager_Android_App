---
phase: 33-data-ai-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MoneyManager/app/build.gradle.kts
  - MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt
autonomous: true
requirements:
  - AIFND-01

must_haves:
  truths:
    - "Gradle sync succeeds with all 4 new dependencies present in build.gradle.kts"
    - "PreferencesManager exposes aiAvailabilityStatus as Flow<String> and setAiAvailabilityStatus(value: String)"
    - "The new DataStore key is stringPreferencesKey(\"ai_availability_status\") — no second dataStore delegate is created"
  artifacts:
    - path: "MoneyManager/app/build.gradle.kts"
      provides: "4 ML Kit + serialization Gradle dependencies"
      contains: "com.google.mlkit:genai-prompt:1.0.0-beta2"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt"
      provides: "ai_availability_status DataStore key and accessor methods"
      contains: "AI_AVAILABILITY_STATUS"
  key_links:
    - from: "PreferencesManager"
      to: "context.dataStore"
      via: "existing single dataStore delegate"
      pattern: "context\\.dataStore\\.data\\.map"
---

<objective>
Add 4 Gradle dependencies required for AI features and extend PreferencesManager with an AI availability status key.

Purpose: All subsequent Phase 33 plans depend on the Gradle sync succeeding before NanoAiClient, DraftParser, PromptBuilder, and DeviceCapabilityManager can be compiled. PreferencesManager must expose the ai_availability_status key before DeviceCapabilityManager (Plan 03) can write to it.

Output: Updated build.gradle.kts with 4 dependencies; PreferencesManager with AI_AVAILABILITY_STATUS key, aiAvailabilityStatus Flow<String>, and setAiAvailabilityStatus(value: String) method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt:
```kotlin
// Single DataStore delegate — MUST NOT create a second one
private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "settings")

class PreferencesManager(private val context: Context) {
    companion object {
        private val DARK_MODE = booleanPreferencesKey("dark_mode")
        private val CURRENCY = stringPreferencesKey("currency")
        // ... other keys follow same pattern
    }
    // Flows use: context.dataStore.data.map { preferences -> preferences[KEY] ?: default }
    // Setters use: context.dataStore.edit { preferences -> preferences[KEY] = value }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 Gradle dependencies to build.gradle.kts</name>
  <files>MoneyManager/app/build.gradle.kts</files>
  <read_first>
    - MoneyManager/app/build.gradle.kts — read the full file to understand the current dependency block structure and where to insert new lines
  </read_first>
  <action>
    In the dependencies block, add the following 4 lines after the DataStore dependency line (implementation("androidx.datastore:datastore-preferences:1.1.1")):

    implementation("com.google.mlkit:genai-prompt:1.0.0-beta2")
    implementation("com.google.mlkit:genai-common:1.0.0-beta3")
    implementation("com.google.android.gms:play-services-mlkit-text-recognition:19.0.1")
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.8.1")

    Also add the kotlinx-serialization Kotlin plugin. In the plugins block, add:
    id("org.jetbrains.kotlin.plugin.serialization")

    Add a comment block above the 4 new lines: // ML Kit GenAI + Serialization (v3.0 AI features)

    Do NOT change any existing dependency strings or version numbers. Do NOT add any other lines.
  </action>
  <verify>
    <automated>cd "D:\Android projec\MoneyManager" && ./gradlew assembleDebug --dry-run 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts contains the exact string: implementation("com.google.mlkit:genai-prompt:1.0.0-beta2")
    - build.gradle.kts contains the exact string: implementation("com.google.mlkit:genai-common:1.0.0-beta3")
    - build.gradle.kts contains the exact string: implementation("com.google.android.gms:play-services-mlkit-text-recognition:19.0.1")
    - build.gradle.kts contains the exact string: implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.8.1")
    - build.gradle.kts contains: id("org.jetbrains.kotlin.plugin.serialization") in the plugins block
    - No existing dependency strings were modified (the original lines remain verbatim)
  </acceptance_criteria>
  <done>build.gradle.kts has all 4 dependencies and the serialization plugin — Gradle sync resolves without errors</done>
</task>

<task type="auto">
  <name>Task 2: Extend PreferencesManager with ai_availability_status key</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/data/preferences/PreferencesManager.kt — read the full file to identify the companion object keys block, the existing Flow properties, and the existing suspend setter functions
  </read_first>
  <action>
    Make 3 additive-only edits to PreferencesManager — do NOT modify any existing line:

    1. In the companion object block, add one new key after the existing LAST_UNLOCK_TIME key:
       private val AI_AVAILABILITY_STATUS = stringPreferencesKey("ai_availability_status")

    2. After the lastUnlockTime Flow property, add one new Flow property:
       val aiAvailabilityStatus: Flow<String> = context.dataStore.data.map { preferences ->
           preferences[AI_AVAILABILITY_STATUS] ?: "PENDING"
       }

    3. After the setLastUnlockTime setter, add one new suspend setter:
       suspend fun setAiAvailabilityStatus(value: String) {
           context.dataStore.edit { preferences ->
               preferences[AI_AVAILABILITY_STATUS] = value
           }
       }

    CRITICAL: Use the existing context.dataStore delegate — do NOT add a second preferencesDataStore declaration. The value parameter type is String, not Boolean.
  </action>
  <verify>
    <automated>grep -n "AI_AVAILABILITY_STATUS\|aiAvailabilityStatus\|setAiAvailabilityStatus" "D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\data\preferences\PreferencesManager.kt"</automated>
  </verify>
  <acceptance_criteria>
    - PreferencesManager.kt contains: private val AI_AVAILABILITY_STATUS = stringPreferencesKey("ai_availability_status")
    - PreferencesManager.kt contains: val aiAvailabilityStatus: Flow<String>
    - PreferencesManager.kt contains: suspend fun setAiAvailabilityStatus(value: String)
    - The file contains exactly 1 occurrence of preferencesDataStore (no second DataStore delegate added)
    - The default value in the aiAvailabilityStatus Flow is "PENDING" (string, not Boolean)
    - All previously existing keys (DARK_MODE, CURRENCY, PIN_ENABLED, etc.) remain unchanged
  </acceptance_criteria>
  <done>PreferencesManager has AI_AVAILABILITY_STATUS key, aiAvailabilityStatus: Flow<String> (defaulting to "PENDING"), and setAiAvailabilityStatus(value: String) — no second DataStore delegate created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore read/write | Preferences are local app storage — no external trust boundary at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | PreferencesManager.setAiAvailabilityStatus | accept | Value is only read by DeviceCapabilityManager (same process); no external input path at this layer |
| T-33-02 | Information Disclosure | ai_availability_status DataStore key | accept | Stored in app-private DataStore; no PII, value is "READY"/"NEVER"/"PENDING" only |
</threat_model>

<verification>
1. Grep build.gradle.kts for all 4 dependency strings — each must be present verbatim
2. Grep PreferencesManager.kt for "preferencesDataStore" — must appear exactly once (no second delegate)
3. Grep PreferencesManager.kt for "AI_AVAILABILITY_STATUS" — must appear in companion object, Flow, and setter
4. Gradle assembleDebug --dry-run exits 0
</verification>

<success_criteria>
- build.gradle.kts has exactly 4 new dependency lines and the serialization plugin — no existing lines changed
- PreferencesManager.kt has exactly 1 new DataStore key (stringPreferencesKey), 1 new Flow<String> property, 1 new suspend setter — all additive, no existing code touched
- Gradle sync resolves successfully
</success_criteria>

<output>
After completion, create .planning/phases/33-data-ai-implementation/33-01-SUMMARY.md
</output>
