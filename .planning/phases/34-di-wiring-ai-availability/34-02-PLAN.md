---
phase: 34-di-wiring-ai-availability
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/MoneyManagerApp.kt
autonomous: true
requirements:
  - AIFND-12

must_haves:
  truths:
    - "MoneyManagerApp.onCreate() launches DeviceCapabilityManager.checkAndCacheAvailability() on Dispatchers.IO — never on the main thread"
    - "The app starts and completes onCreate() without blocking — CoroutineScope(Dispatchers.IO).launch returns immediately on the main thread"
    - "DeviceCapabilityManager is field-injected by Hilt via @Inject lateinit var — not manually constructed"
    - "The existing appLockManager injection and registerActivityLifecycleCallbacks call are unchanged"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/MoneyManagerApp.kt"
      provides: "Updated Application class with DeviceCapabilityManager field injection and async IO launch"
      contains: "deviceCapabilityManager.checkAndCacheAvailability()"
  key_links:
    - from: "MoneyManagerApp.onCreate()"
      to: "DeviceCapabilityManager.checkAndCacheAvailability()"
      via: "CoroutineScope(Dispatchers.IO).launch"
      pattern: "Dispatchers\\.IO.*launch\\|launch.*Dispatchers\\.IO"
    - from: "Hilt @Inject"
      to: "DeviceCapabilityManager"
      via: "@Inject lateinit var deviceCapabilityManager"
      pattern: "@Inject.*deviceCapabilityManager"
---

<objective>
Add DeviceCapabilityManager field injection and async IO startup hook to MoneyManagerApp.

Purpose: AICore availability must be checked once at app startup and cached in PreferencesManager so subsequent launches can skip the blocking IPC call. The check MUST run on Dispatchers.IO — it performs blocking IPC to the AICore system service. Blocking the main thread here would cause an ANR.

Output: Updated MoneyManagerApp.kt with two additions: a new @Inject field and a CoroutineScope(Dispatchers.IO).launch block in onCreate().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-di-wiring-ai-availability/34-01-SUMMARY.md

<interfaces>
Current MoneyManagerApp.kt (full file — 17 lines, read before editing):
```kotlin
package com.moneymanager.app

import android.app.Application
import com.moneymanager.app.ui.util.AppLockManager
import dagger.hilt.android.HiltAndroidApp
import javax.inject.Inject

@HiltAndroidApp
class MoneyManagerApp : Application() {
    @Inject
    lateinit var appLockManager: AppLockManager

    override fun onCreate() {
        super.onCreate()
        registerActivityLifecycleCallbacks(appLockManager)
    }
}
```

From MoneyManager/app/src/main/java/com/moneymanager/data/ai/DeviceCapabilityManager.kt:
```kotlin
class DeviceCapabilityManager(
    private val context: Context,
    private val preferencesManager: PreferencesManager
) {
    suspend fun checkAndCacheAvailability() { ... }
}
```

NOTE — DeviceCapabilityManager is provided by AiModule (Plan 34-01) as a @Singleton. It is eligible for field injection via @Inject in a @HiltAndroidApp Application subclass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeviceCapabilityManager injection and async IO startup hook to MoneyManagerApp</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/MoneyManagerApp.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/MoneyManagerApp.kt — read the full current file (17 lines) before making any edits, to confirm the existing appLockManager field and onCreate() body have not changed from the version shown in the interfaces block above
  </read_first>
  <action>
    Modify MoneyManagerApp.kt with exactly two additive changes. Do not remove or reorder any existing lines.

    CHANGE 1 — Add import statements (add after the existing imports, before the @HiltAndroidApp annotation):
    Add these three new imports:
    - import com.moneymanager.data.ai.DeviceCapabilityManager
    - import kotlinx.coroutines.CoroutineScope
    - import kotlinx.coroutines.Dispatchers
    - import kotlinx.coroutines.launch

    CHANGE 2 — Add field injection for DeviceCapabilityManager (add AFTER the existing appLockManager field, before the override fun onCreate block):
    ```
    @Inject
    lateinit var deviceCapabilityManager: DeviceCapabilityManager
    ```

    CHANGE 3 — Add the async launch in onCreate() (add as the LAST statement inside onCreate(), after the existing registerActivityLifecycleCallbacks call):
    ```
    CoroutineScope(Dispatchers.IO).launch {
        deviceCapabilityManager.checkAndCacheAvailability()
    }
    ```

    CRITICAL constraints:
    - The CoroutineScope(Dispatchers.IO).launch block MUST be the last statement in onCreate() — never before super.onCreate() or before registerActivityLifecycleCallbacks.
    - Do NOT use runBlocking — that would block the main thread.
    - Do NOT call checkAndCacheAvailability() directly (without a coroutine scope) — it is a suspend function and calling it synchronously on the main thread would cause an ANR.
    - Do NOT use GlobalScope — it is discouraged. Use CoroutineScope(Dispatchers.IO) which is fire-and-forget appropriate for an Application-scoped one-shot task.
    - The existing appLockManager @Inject field and registerActivityLifecycleCallbacks call must remain exactly as-is.

    The completed file after changes:

    package com.moneymanager.app

    import android.app.Application
    import com.moneymanager.app.ui.util.AppLockManager
    import com.moneymanager.data.ai.DeviceCapabilityManager
    import dagger.hilt.android.HiltAndroidApp
    import kotlinx.coroutines.CoroutineScope
    import kotlinx.coroutines.Dispatchers
    import kotlinx.coroutines.launch
    import javax.inject.Inject

    @HiltAndroidApp
    class MoneyManagerApp : Application() {
        @Inject
        lateinit var appLockManager: AppLockManager

        @Inject
        lateinit var deviceCapabilityManager: DeviceCapabilityManager

        override fun onCreate() {
            super.onCreate()
            registerActivityLifecycleCallbacks(appLockManager)
            CoroutineScope(Dispatchers.IO).launch {
                deviceCapabilityManager.checkAndCacheAvailability()
            }
        }
    }
  </action>
  <verify>
    <automated>grep -n "@Inject\|deviceCapabilityManager\|Dispatchers.IO\|CoroutineScope\|launch\|checkAndCacheAvailability\|runBlocking" "D:\Android projec\MoneyManager\app\src\main\java\com\moneymanager\app\MoneyManagerApp.kt"</automated>
  </verify>
  <acceptance_criteria>
    - MoneyManagerApp.kt contains: @Inject followed by lateinit var deviceCapabilityManager: DeviceCapabilityManager
    - MoneyManagerApp.kt contains: CoroutineScope(Dispatchers.IO).launch
    - MoneyManagerApp.kt contains: deviceCapabilityManager.checkAndCacheAvailability()
    - MoneyManagerApp.kt contains: import com.moneymanager.data.ai.DeviceCapabilityManager
    - MoneyManagerApp.kt contains: import kotlinx.coroutines.CoroutineScope
    - MoneyManagerApp.kt contains: import kotlinx.coroutines.Dispatchers
    - MoneyManagerApp.kt contains: import kotlinx.coroutines.launch
    - MoneyManagerApp.kt does NOT contain: runBlocking (forbidden — blocks main thread)
    - MoneyManagerApp.kt still contains: lateinit var appLockManager: AppLockManager (existing field unchanged)
    - MoneyManagerApp.kt still contains: registerActivityLifecycleCallbacks(appLockManager) (existing call unchanged)
  </acceptance_criteria>
  <done>MoneyManagerApp.onCreate() launches checkAndCacheAvailability() on Dispatchers.IO after registerActivityLifecycleCallbacks — the call is non-blocking on the main thread and Hilt injects DeviceCapabilityManager as a @Singleton provided by AiModule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main thread → AICore IPC | checkAndCacheAvailability() performs blocking IPC; must never run on main thread |
| Application onCreate() → DeviceCapabilityManager | Hilt injects DeviceCapabilityManager before super.onCreate() returns; field is guaranteed non-null when launch executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Denial of Service | MoneyManagerApp — checkAndCacheAvailability on main thread | mitigate | CoroutineScope(Dispatchers.IO).launch dispatches to IO thread pool immediately; main thread returns without blocking |
| T-34-06 | Denial of Service | MoneyManagerApp — runBlocking misuse | mitigate | Acceptance criteria explicitly verify runBlocking is absent from the file; any use of runBlocking fails the grep check |
| T-34-07 | Tampering | MoneyManagerApp — appLockManager behavior changed | mitigate | Acceptance criteria verify registerActivityLifecycleCallbacks(appLockManager) is still present; existing security lock behavior is unchanged |
</threat_model>

<verification>
1. grep -n "Dispatchers.IO" app/MoneyManagerApp.kt — must match (IO dispatcher used)
2. grep -c "runBlocking" app/MoneyManagerApp.kt — must return 0 (main-thread block forbidden)
3. grep -n "checkAndCacheAvailability" app/MoneyManagerApp.kt — must match (inside the launch block)
4. grep -n "deviceCapabilityManager" app/MoneyManagerApp.kt — must show @Inject field and usage in launch block
5. grep -n "registerActivityLifecycleCallbacks" app/MoneyManagerApp.kt — must still match (existing behavior preserved)
6. grep -n "appLockManager" app/MoneyManagerApp.kt — must still match (existing @Inject field preserved)
</verification>

<success_criteria>
- MoneyManagerApp.kt has the DeviceCapabilityManager @Inject field and launches checkAndCacheAvailability() on Dispatchers.IO in onCreate()
- The main thread is never blocked — no runBlocking, no direct suspend call on main thread
- The existing appLockManager field injection and registerActivityLifecycleCallbacks call are preserved exactly
- The app compiles and starts on both AICore and non-AICore devices: on non-AICore, the launch executes checkAndCacheAvailability() which writes "NEVER" to PreferencesManager (per DeviceCapabilityManager catch block) and the app continues normally
</success_criteria>

<output>
After completion, create .planning/phases/34-di-wiring-ai-availability/34-02-SUMMARY.md
</output>
