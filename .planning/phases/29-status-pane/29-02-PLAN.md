---
phase: 29-status-pane
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - ui/insights/status/StatusPaneScreen.kt
autonomous: true
requirements: [STA-01, STA-02, STA-03, STA-04, STA-05, STA-06, STA-07, STA-08, STA-09]
must_haves:
  truths:
    - StatusPaneScreen displays StatusPaneHero, StatusPaneFigureGrid, or StatusPaneEmptyState based on uiState
    - StatusPaneScreen observes InsightsViewModel's StateFlow&amp;lt;InsightsUiState&amp;gt;
    - Empty state shown when hasTransactions is false
    - All monetary figures display currency symbol
  artifacts:
    - path: "ui/insights/status/StatusPaneScreen.kt"
      provides: "Root status pane screen container"
      contains: "fun StatusPaneScreen"
    - path: "ui/insights/status/StatusPaneScreen.kt"
      provides: "ViewModel integration"
      contains: "viewModel: InsightsViewModel"
  key_links:
    - from: "StatusPaneScreen.kt"
      to: "InsightsViewModel"
      via: "viewModel.uiState.collectAsStateWithLifecycle()"
    - from: "StatusPaneScreen.kt"
      to: "StatusUiState"
      via: "uiState.statusUiState"
---

<objective>
Create StatusPaneScreen root composable and wire to InsightsViewModel.
Purpose: Connect the stateless composables from 29-01 to live data from the ViewModel, handle state transitions (loading, success, empty).
Output: Root StatusPaneScreen composable that displays correct UI based on current financial data.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-status-pane/029-UI-SPEC.md
@.planning/REQUIREMENTS.md
@ui/insights/status/StatusPaneHero.kt (from 29-01)
@ui/insights/status/StatusPaneFigureGrid.kt (from 29-01)
@ui/insights/status/StatusPaneEmptyState.kt (from 29-01)

<interfaces>
From Phase 27:
```kotlin
class InsightsViewModel @Inject constructor(
    private val calculator: InsightsCalculator,
    private val repository: TransactionRepository
) : ViewModel() {
    val uiState: StateFlow&amp;lt;InsightsUiState&amp;gt; = ...
}

data class InsightsUiState(
    val statusUiState: StatusUiState,
    val risksUiState: RisksUiState,
    val trendsUiState: TrendsUiState
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create StatusPaneScreen composable with ViewModel wiring</name>
  <files>ui/insights/status/StatusPaneScreen.kt</files>
  <action>Create StatusPaneScreen composable that:
  1. Accepts viewModel: InsightsViewModel via hiltViewModel()
  2. Collects uiState as state with collectAsStateWithLifecycle()
  3. Extracts statusUiState: StatusUiState from uiState.statusUiState
  4. Renders scrollable Column:
     - If statusUiState.hasTransactions is false: StatusPaneEmptyState()
     - Else: StatusPaneHero (monthYear, netPosition, currencySymbol), vertical spacing (16dp), StatusPaneFigureGrid (list of 6 figures)
  5. Uses Material 3 Scaffold? No, InsightsScreen will provide Scaffold. StatusPaneScreen is a page in HorizontalPager, so no Scaffold.
  Follow existing ViewModel integration patterns from DashboardScreen.kt.</action>
  <verify>
    <automated>ls ui/insights/status/StatusPaneScreen.kt 2>$null && grep -c "collectAsStateWithLifecycle" ui/insights/status/StatusPaneScreen.kt</automated>
  </verify>
  <done>StatusPaneScreen exists, wires to InsightsViewModel, displays correct content based on hasTransactions flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ViewModel → UI | Local StateFlow, no external data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | I | StatusPaneScreen | accept | Only observes local ViewModel, no external data input |
</threat_model>

<verification>
1. StatusPaneScreen compiles and wires to InsightsViewModel correctly
2. Empty state displayed when hasTransactions is false
3. Hero and figure grid displayed when hasTransactions is true
4. All monetary figures include currency symbol
</verification>

<success_criteria>
StatusPaneScreen correctly observes ViewModel state and renders appropriate UI for empty/has data states.
</success_criteria>

<output>
After completion, create `.planning/phases/29-status-pane/29-02-SUMMARY.md`
</output>
