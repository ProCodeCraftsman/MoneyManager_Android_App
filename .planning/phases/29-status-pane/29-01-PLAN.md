---
phase: 29-status-pane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/insights/status/StatusPaneHero.kt
  - ui/insights/status/StatusPaneFigure.kt
  - ui/insights/status/StatusPaneFigureGrid.kt
  - ui/insights/status/StatusPaneEmptyState.kt
autonomous: true
requirements: [STA-01, STA-02, STA-03, STA-04, STA-05, STA-06, STA-07, STA-08, STA-09]
must_haves:
  truths:
    - StatusPaneHero displays net position as largest hero number with month/year label and currency symbol
    - StatusPaneFigure displays labeled monetary value with currency symbol
    - StatusPaneFigureGrid arranges 6 StatusPaneFigure items in 2-column grid
    - StatusPaneEmptyState displays "No financial activity recorded yet" copy
  artifacts:
    - path: "ui/insights/status/StatusPaneHero.kt"
      provides: "Net position hero rendering"
      contains: "fun StatusPaneHero"
    - path: "ui/insights/status/StatusPaneFigure.kt"
      provides: "Single labeled figure rendering"
      contains: "fun StatusPaneFigure"
    - path: "ui/insights/status/StatusPaneFigureGrid.kt"
      provides: "2-column figure grid container"
      contains: "fun StatusPaneFigureGrid"
    - path: "ui/insights/status/StatusPaneEmptyState.kt"
      provides: "Empty state view"
      contains: "fun StatusPaneEmptyState"
  key_links:
    - from: "StatusPaneHero.kt"
      to: "StatusUiState"
      via: "accepts monthYear: String, netPosition: BigDecimal, currencySymbol: String params"
    - from: "StatusPaneFigure.kt"
      to: "StatusUiState"
      via: "accepts label: String, value: BigDecimal, currencySymbol: String params"
---

<objective>
Create stateless Jetpack Compose composables for the Status Pane UI components per 029-UI-SPEC.md.
Purpose: Implement the visual building blocks for displaying financial position summary, following Material 3 design guidelines and existing project patterns.
Output: 4 stateless composable files ready for integration into the root StatusPaneScreen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-status-pane/029-UI-SPEC.md
@.planning/REQUIREMENTS.md

<interfaces>
From Phase 27 (InsightsUiState):
```kotlin
data class StatusUiState(
    val monthYear: String,
    val netPosition: BigDecimal,
    val currencySymbol: String,
    val netCashFlow: BigDecimal,
    val totalIncome: BigDecimal,
    val totalExpense: BigDecimal,
    val totalSavings: BigDecimal,
    val totalLending: BigDecimal,
    val totalBorrowing: BigDecimal,
    val hasTransactions: Boolean
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create StatusPaneHero, StatusPaneFigure, StatusPaneFigureGrid, StatusPaneEmptyState composables</name>
  <files>ui/insights/status/StatusPaneHero.kt, ui/insights/status/StatusPaneFigure.kt, ui/insights/status/StatusPaneFigureGrid.kt, ui/insights/status/StatusPaneEmptyState.kt</files>
  <action>Create 4 stateless Jetpack Compose composables per 029-UI-SPEC.md:
  1. StatusPaneHero: Accepts monthYear: String, netPosition: BigDecimal, currencySymbol: String. Renders month/year label (headlineMedium), net position (displaySmall, colored green if positive, red if negative), currency symbol (primary color). Follow Material 3 typography tokens from UI-SPEC.
  2. StatusPaneFigure: Accepts label: String, value: BigDecimal, currencySymbol: String. Renders label (bodyLarge) and value (titleLarge) with currency symbol prefix.
  3. StatusPaneFigureGrid: Accepts figures: List&amp;lt;StatusFigure&amp;gt; (6 items: Net Cash Flow, Income, Expense, Savings, Lending, Borrowing). Renders 2-column LazyVerticalGrid with StatusPaneFigure for each item.
  4. StatusPaneEmptyState: No params. Renders full-screen centered column with "No financial activity recorded yet" copy (bodyLarge), matches UI-SPEC copy exactly.
  Use Material 3 components (Text, LazyVerticalGrid, Column, Row) only. No internal state, all data via parameters. Follow existing project composable patterns (see DashboardScreen.kt for reference).</action>
  <verify>
    <automated>ls ui/insights/status/StatusPaneHero.kt ui/insights/status/StatusPaneFigure.kt ui/insights/status/StatusPaneFigureGrid.kt ui/insights/status/StatusPaneEmptyState.kt 2>$null | wc -l</automated>
  </verify>
  <done>All 4 composable files exist, each has a fun Composable with correct params, follows UI-SPEC typography/spacing rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local UI | All data from local ViewModel, no external inputs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I | All Composables | accept | Only render user's own financial data from local Room database |
| T-29-02 | D | All Composables | accept | No external dependencies, static UI components |
</threat_model>

<verification>
1. All composables compile without errors
2. UI-SPEC typography (displaySmall for hero, titleLarge for figures, bodyLarge for labels) is followed
3. 2-column grid uses 16dp spacing per UI-SPEC
4. Empty state copy matches STA-09 exactly
</verification>

<success_criteria>
4 stateless composable files created, matching UI-SPEC.md requirements, no internal state, all data via parameters.
</success_criteria>

<output>
After completion, create `.planning/phases/29-status-pane/29-01-SUMMARY.md`
</output>
