---
phase: 29-status-pane
plan: 03
type: execute
wave: 3
depends_on: [29-02, 28-01]
files_modified:
  - ui/insights/InsightsScreen.kt
autonomous: true
requirements: [STA-01, STA-02, STA-03, STA-04, STA-05, STA-06, STA-07, STA-08, STA-09]
must_haves:
  truths:
    - StatusPaneScreen is displayed as the first page in InsightsScreen's HorizontalPager
    - Swiping to first page (index 0) shows Status Pane content
  artifacts:
    - path: "ui/insights/InsightsScreen.kt"
      provides: "Insights screen with 3 panes"
      contains: "StatusPaneScreen"
  key_links:
    - from: "InsightsScreen.kt"
      to: "StatusPaneScreen.kt"
      via: "HorizontalPager page content"
---

<objective>
Integrate StatusPaneScreen into InsightsScreen as the first page of the HorizontalPager.
Purpose: Make the Status Pane accessible to users via the Insights section navigation, completing Phase 29's integration.
Output: Updated InsightsScreen with StatusPaneScreen as the first pane.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-status-pane/029-UI-SPEC.md
@ui/insights/InsightsScreen.kt (from Phase 28 28-01-PLAN)
@ui/insights/status/StatusPaneScreen.kt (from 29-02)
</context>

<tasks>

<task type="auto">
  <name>task 1: update InsightsScreen to include StatusPaneScreen as first page</name>
  <files>ui/insights/InsightsScreen.kt</files>
  <action>Modify InsightsScreen's HorizontalPager content to replace the first page placeholder with StatusPaneScreen:
  1. In HorizontalPager's page content lambda, for page index 0, return StatusPaneScreen()
  2. Keep placeholders for pages 1 (Risks) and 2 (Trends) as they are (to be replaced in Phases 30 and 31)
  3. Ensure TabRow indicator updates correctly when first page is selected
  Follow existing HorizontalPager patterns from Phase 28's 28-01-PLAN.</action>
  <verify>
    <automated>grep -c "StatusPaneScreen()" ui/insights/InsightsScreen.kt 2>$null</automated>
  </verify>
  <done>InsightsScreen's first page renders StatusPaneScreen, HorizontalPager swipes correctly to first page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| InsightsScreen → StatusPaneScreen | Local composable call, no data transfer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | I | InsightsScreen | accept | No data transfer, only composable integration |
</threat_model>

<verification>
1. InsightsScreen compiles with StatusPaneScreen as first page
2. HorizontalPager correctly renders StatusPaneScreen for page 0
3. TabRow indicator shows first tab as selected initially
</verification>

<success_criteria>
StatusPaneScreen integrated into InsightsScreen as the first pane, accessible via bottom nav and swipe.
</success_criteria>

<output>
After completion, create `.planning/phases/29-status-pane/29-03-SUMMARY.md`
</output>
