---
phase: 32-domain-ai-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionDraft.kt
  - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/PromptContext.kt
autonomous: true
requirements:
  - AIFND-05
  - AIFND-07

must_haves:
  truths:
    - "TransactionDraft carries resolved entity IDs (categoryId, accountId, peerContactId) and does not extend TransactionEntity"
    - "PromptContext wraps four entry-type lists: CategoryEntry, AccountEntry, PeerEntry, TagEntry"
    - "All four entry types are pure Kotlin data classes with no Room or Android annotations"
    - "Zero android.* or androidx.* imports exist in either created file"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionDraft.kt"
      provides: "Domain model for AI-suggested transaction pre-fill data"
      contains: "data class TransactionDraft"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/domain/ai/PromptContext.kt"
      provides: "Prompt context data contract and projection entry types"
      contains: "data class PromptContext"
  key_links:
    - from: "TransactionDraft.typeId"
      to: "TransactionType.id"
      via: "string equality — validated by GenerateDraftFromTextUseCase in Plan 03"
      pattern: "typeId: String"
    - from: "PromptContextBuilder (Plan 03)"
      to: "CategoryEntry, AccountEntry, PeerEntry, TagEntry"
      via: "constructs PromptContext from these projection types — NOT from Room entities"
      pattern: "PromptContext(top20Categories = ..., accounts = ..., peers = ..., tags = ...)"
---

<objective>
Create TransactionDraft (the AI output model) and PromptContext with its four entry projection types (the AI input model). These are the data-contract files that sit between the AI generation layer and the UI layer — both must be pure Kotlin with no Room or Android dependencies.

Purpose: Defines what the AI produces (TransactionDraft) and what it consumes (PromptContext) in type-safe domain terms, decoupled from Room entities.
Output: Two new files in com.moneymanager.domain.ai; no existing files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Types created in Plan 01 that these files may reference -->

From MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt (created in Plan 01):
  enum class TransactionType(
      val id: String,
      val displayName: String,
      val promptHint: String,
      val requiresCategory: Boolean,
      val requiresPeer: Boolean
  )
  companion object {
      fun allIds(): List<String>
      fun fromId(id: String): TransactionType?
  }

<!-- TransactionDraft stores typeId as String (not TransactionType enum) because -->
<!-- the AI may return an unrecognized type — caller validates via TransactionType.fromId() -->

<!-- Room entities (DO NOT import or reference these in domain/ai/ files): -->
<!-- CategoryEntity, AccountEntity, PeerContact, TagEntity all have @Entity or -->
<!-- @Parcelize or android.* imports — they must NOT appear in domain/ai/ files -->

<!-- Existing repository interfaces show what field names are in the Room entities: -->
<!-- CategoryEntity: id: Long, name: String, type: String (income/expense) -->
<!-- AccountEntity: id: Long, name: String -->
<!-- PeerContact: id: Long, name: String -->
<!-- TagEntity: id: Long, name: String -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TransactionDraft data class</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionDraft.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/data/entity/TransactionEntity.kt
    MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt
  </read_first>
  <action>
    Create file at package com.moneymanager.domain.ai. Declare data class TransactionDraft with the following nullable fields (all default to null so partial drafts are valid):

    - typeId: String? — raw type string from AI; callers validate via TransactionType.fromId(). Do NOT use TransactionType enum as the field type.
    - amount: Double? — always positive; null means AI did not extract an amount
    - categoryId: Long? — resolved category ID; null if AI could not match a category
    - categoryName: String? — display name from AI output (for UI rendering before ID is validated)
    - accountId: Long? — resolved account ID; null if AI could not match an account
    - accountName: String? — display name from AI output
    - peerContactId: Long? — resolved peer ID; null if not a peer transaction type
    - peerContactName: String? — display name from AI output
    - tagIds: List<Long> — resolved tag IDs; empty list (not null) when no tags extracted. Use default value emptyList()
    - description: String? — short description extracted by AI
    - note: String? — longer note extracted by AI
    - date: Long? — epoch-millis extracted by AI; null if not present in source text
    - sourceType: String? — origin label for UI banner (e.g., "SMS", "OCR", "VOICE")
    - sourceSender: String? — sender ID or label (e.g., "HDFCBK") for UI banner display

    TransactionDraft must NOT extend TransactionEntity or any Room entity class. It is a standalone data class. No @Parcelize, no @Serializable (those go on the data layer DTO in Phase 33/36). No android.* or androidx.* imports.
  </action>
  <verify>
    <automated>cd "D:/Android projec/MoneyManager" && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionDraft.kt
    - File contains "data class TransactionDraft"
    - File does NOT contain "extends TransactionEntity" or ": TransactionEntity"
    - File contains fields: typeId, amount, categoryId, categoryName, accountId, accountName, peerContactId, peerContactName, tagIds, description, note, date, sourceType, sourceSender
    - tagIds field has type List&lt;Long&gt; with default value emptyList()
    - File has zero lines containing "import android.", "import androidx.", "import com.google.", "@Entity", "@Parcelize", "@Serializable"
    - Kotlin compilation succeeds (gradlew compileDebugKotlin exits 0)
  </acceptance_criteria>
  <done>TransactionDraft.kt compiles; all specified fields present with correct types; zero Android/Room imports</done>
</task>

<task type="auto">
  <name>Task 2: Create PromptContext with projection entry types</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/domain/ai/PromptContext.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/domain/repository/CategoryRepository.kt
    MoneyManager/app/src/main/java/com/moneymanager/domain/repository/AccountRepository.kt
    MoneyManager/app/src/main/java/com/moneymanager/domain/repository/PeerContactRepository.kt
  </read_first>
  <action>
    Create file at package com.moneymanager.domain.ai. Declare four pure-Kotlin data classes as projection types (these shadow the Room entity fields without carrying Room annotations):

    data class CategoryEntry(val id: Long, val name: String, val type: String)
    data class AccountEntry(val id: Long, val name: String)
    data class PeerEntry(val id: Long, val name: String)
    data class TagEntry(val id: Long, val name: String)

    Then declare the wrapper:
    data class PromptContext(
        val top20Categories: List&lt;CategoryEntry&gt;,
        val accounts: List&lt;AccountEntry&gt;,
        val peers: List&lt;PeerEntry&gt;,
        val tags: List&lt;TagEntry&gt;
    )

    All five declarations (CategoryEntry, AccountEntry, PeerEntry, TagEntry, PromptContext) may live in a single file. No @Entity, @Parcelize, or @Serializable annotations. No android.* or androidx.* imports. No kotlinx.* imports.
  </action>
  <verify>
    <automated>cd "D:/Android projec/MoneyManager" && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at MoneyManager/app/src/main/java/com/moneymanager/domain/ai/PromptContext.kt
    - File contains "data class CategoryEntry" with fields id: Long, name: String, type: String
    - File contains "data class AccountEntry" with fields id: Long, name: String
    - File contains "data class PeerEntry" with fields id: Long, name: String
    - File contains "data class TagEntry" with fields id: Long, name: String
    - File contains "data class PromptContext" with fields: top20Categories: List&lt;CategoryEntry&gt;, accounts: List&lt;AccountEntry&gt;, peers: List&lt;PeerEntry&gt;, tags: List&lt;TagEntry&gt;
    - File has zero lines containing "import android.", "import androidx.", "import com.google.", "import kotlinx.", "@Entity", "@Parcelize"
    - Kotlin compilation succeeds (gradlew compileDebugKotlin exits 0)
  </acceptance_criteria>
  <done>PromptContext.kt compiles; all 5 data classes present with correct field types; zero Android/Room imports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI output → TransactionDraft | Raw AI JSON is parsed into TransactionDraft; all fields are nullable to handle partial/invalid AI responses |
| TransactionDraft → AddEditTransactionDialog | Dialog trusts TransactionDraft field values for pre-fill but passes through the existing save validation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | TransactionDraft.typeId | mitigate | typeId is String? not TransactionType enum — GenerateDraftFromTextUseCase (Plan 03) validates via TransactionType.fromId() before acting; invalid types are surfaced as parse error, not silently accepted |
| T-32-05 | Information Disclosure | PromptContext.peers | accept | Peer names are master data the user already entered; no external data introduced. Phase 33 PromptBuilder sanitizes before prompt injection |
| T-32-06 | Tampering | TransactionDraft.amount | accept | Amount is Double? — always positive per domain convention; sign is derived from typeId at save time via existing validation path (DRAFT-09) |
</threat_model>

<verification>
After both tasks complete:
1. Both files exist under MoneyManager/app/src/main/java/com/moneymanager/domain/ai/
2. Import scan: grep -r "import android\.\|import androidx\.\|import com\.google\.\|import kotlinx\." MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionDraft.kt MoneyManager/app/src/main/java/com/moneymanager/domain/ai/PromptContext.kt returns zero matches
3. gradlew :app:compileDebugKotlin exits 0 with all 5 new domain/ai/ files present
</verification>

<success_criteria>
- TransactionDraft.kt: 13 nullable/defaulted fields, no Room/Android imports, does not extend any entity
- PromptContext.kt: 4 projection data classes + PromptContext wrapper, no imports at all
- Kotlin compilation succeeds with all 5 domain/ai/ files (from Plans 01 and 02) present
</success_criteria>

<output>
After completion, create .planning/phases/32-domain-ai-foundation/32-02-SUMMARY.md
</output>
