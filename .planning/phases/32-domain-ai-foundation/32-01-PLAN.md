---
phase: 32-domain-ai-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt
  - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/GenAiClient.kt
  - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/AiUnavailableException.kt
autonomous: true
requirements:
  - AIFND-03
  - AIFND-06
  - AIFND-08

must_haves:
  truths:
    - "TransactionType enum has exactly 8 entries whose ids match TransactionEntity.VALID_TYPES strings verbatim"
    - "GenAiClient interface exposes exactly one method: generateDraft(prompt: String): Result<String>"
    - "AiUnavailableException is a domain exception class with no Android runtime dependency"
    - "Zero android.* or androidx.* imports exist in any of the three created files"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt"
      provides: "Single source of truth for transaction type strings in all new AI code"
      contains: "enum class TransactionType"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/domain/ai/GenAiClient.kt"
      provides: "AI text generation contract — single method interface"
      contains: "interface GenAiClient"
    - path: "MoneyManager/app/src/main/java/com/moneymanager/domain/ai/AiUnavailableException.kt"
      provides: "Domain exception signalling AI client is absent"
      contains: "class AiUnavailableException"
  key_links:
    - from: "TransactionType entries"
      to: "TransactionEntity.VALID_TYPES"
      via: "id property value equality"
      pattern: "id = \"income\"|\"expense\"|\"savings\"|\"transfer\"|\"lend\"|\"receive\"|\"borrow\"|\"repay\""
---

<objective>
Create the three foundational pure-Kotlin domain contracts: TransactionType enum, GenAiClient interface, and AiUnavailableException. These are the leaf-level declarations on which every other Phase 32 file depends.

Purpose: Establishes the type-safe entry points for all AI domain logic — zero Android runtime imports so these compile and unit-test on plain JVM.
Output: Three new files in com.moneymanager.domain.ai; no existing files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Source-of-truth strings this plan must replicate exactly. Extracted from: -->
<!-- MoneyManager/app/src/main/java/com/moneymanager/data/entity/TransactionEntity.kt -->

TransactionEntity.VALID_TYPES (line 60):
  val VALID_TYPES = listOf("income", "expense", "savings", "transfer", "lend", "receive", "borrow", "repay")

<!-- These 8 strings are the required `id` values for the 8 TransactionType enum entries. -->
<!-- Do NOT alter them. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TransactionType enum</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt</files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/data/entity/TransactionEntity.kt
  </read_first>
  <behavior>
    - TransactionType.INCOME.id == "income"
    - TransactionType.EXPENSE.id == "expense"
    - TransactionType.SAVINGS.id == "savings"
    - TransactionType.TRANSFER.id == "transfer"
    - TransactionType.LEND.id == "lend"
    - TransactionType.RECEIVE.id == "receive"
    - TransactionType.BORROW.id == "borrow"
    - TransactionType.REPAY.id == "repay"
    - TransactionType.allIds() returns a list of all 8 id strings in declaration order
    - TransactionType.fromId("income") returns TransactionType.INCOME (not null)
    - TransactionType.fromId("unknown") returns null
    - entries with requiresCategory=true: INCOME, EXPENSE, SAVINGS
    - entries with requiresPeer=true: LEND, RECEIVE, BORROW, REPAY
  </behavior>
  <action>
    Create file at package com.moneymanager.domain.ai. Declare enum class TransactionType with constructor parameters: id: String, displayName: String, promptHint: String, requiresCategory: Boolean, requiresPeer: Boolean.

    Declare 8 entries in this order: INCOME, EXPENSE, SAVINGS, TRANSFER, LEND, RECEIVE, BORROW, REPAY. Each entry's id must match the corresponding string from TransactionEntity.VALID_TYPES exactly (case-sensitive).

    Suggested displayName values: "Income", "Expense", "Savings", "Transfer", "Lend", "Receive", "Borrow", "Repay".

    Suggested promptHint values (used by PromptBuilder in Phase 33 to guide AI): "money received (salary, business income)", "money spent (bills, shopping, food)", "money saved or invested", "money moved between own accounts", "money lent to someone", "money received back from someone you lent to", "money borrowed from someone", "repayment of borrowed money".

    requiresCategory: true for INCOME, EXPENSE, SAVINGS; false for TRANSFER, LEND, RECEIVE, BORROW, REPAY.
    requiresPeer: true for LEND, RECEIVE, BORROW, REPAY; false for INCOME, EXPENSE, SAVINGS, TRANSFER.

    Add companion object with:
      fun allIds(): List<String> — returns entries.map { it.id }
      fun fromId(id: String): TransactionType? — returns entries.firstOrNull { it.id == id }

    Zero android.*, androidx.*, or com.google.* imports. No other imports are needed.
  </action>
  <verify>
    <automated>cd "D:/Android projec/MoneyManager" && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at MoneyManager/app/src/main/java/com/moneymanager/domain/ai/TransactionType.kt
    - File contains "enum class TransactionType"
    - File contains exactly 8 enum entries: INCOME, EXPENSE, SAVINGS, TRANSFER, LEND, RECEIVE, BORROW, REPAY
    - Each entry has id, displayName, promptHint, requiresCategory, requiresPeer constructor args
    - id values are exactly: "income", "expense", "savings", "transfer", "lend", "receive", "borrow", "repay"
    - companion object contains fun allIds() and fun fromId(id: String): TransactionType?
    - File has zero lines containing "import android.", "import androidx.", "import com.google."
    - Kotlin compilation succeeds (gradlew compileDebugKotlin exits 0)
  </acceptance_criteria>
  <done>TransactionType.kt compiled, 8 entries with correct ids, allIds() and fromId() present, zero Android imports</done>
</task>

<task type="auto">
  <name>Task 2: Create GenAiClient interface and AiUnavailableException</name>
  <files>
    MoneyManager/app/src/main/java/com/moneymanager/domain/ai/GenAiClient.kt
    MoneyManager/app/src/main/java/com/moneymanager/domain/ai/AiUnavailableException.kt
  </files>
  <read_first>
    MoneyManager/app/src/main/java/com/moneymanager/domain/repository/CategoryRepository.kt
  </read_first>
  <action>
    GenAiClient.kt — package com.moneymanager.domain.ai. Declare interface GenAiClient with exactly one method: suspend fun generateDraft(prompt: String): Result&lt;String&gt;. No other methods. No imports — Result is kotlin.Result (stdlib, no import needed in Kotlin). Zero android.*, androidx.*, or com.google.* imports.

    AiUnavailableException.kt — package com.moneymanager.domain.ai. Declare class AiUnavailableException extending Exception. Provide a no-arg constructor and a single-arg constructor that accepts a message: String (default value: "AI client is not available"). This class is thrown by GenerateDraftFromTextUseCase (created in Plan 03) when the injected GenAiClient? is null. Zero android.*, androidx.*, or com.google.* imports.
  </action>
  <verify>
    <automated>cd "D:/Android projec/MoneyManager" && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/GenAiClient.kt exists
    - GenAiClient.kt contains "interface GenAiClient"
    - GenAiClient.kt contains exactly one method declaration: "suspend fun generateDraft(prompt: String): Result<String>"
    - MoneyManager/app/src/main/java/com/moneymanager/domain/ai/AiUnavailableException.kt exists
    - AiUnavailableException.kt contains "class AiUnavailableException"
    - AiUnavailableException extends Exception
    - Both files have zero lines containing "import android.", "import androidx.", "import com.google."
    - Kotlin compilation succeeds (gradlew compileDebugKotlin exits 0)
  </acceptance_criteria>
  <done>GenAiClient.kt and AiUnavailableException.kt compile successfully; both are pure Kotlin with no Android imports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain/ai → caller | Use case returns Result — callers must handle failure without crashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | TransactionType.id strings | accept | Enum values are compile-time constants; strings match VALID_TYPES which are also constants — no runtime mutation path |
| T-32-02 | Information Disclosure | GenAiClient.generateDraft | accept | Interface declares no logging; implementations (Phase 33/34) must not log raw prompt — noted in Phase 33 plan |
| T-32-03 | Denial of Service | AiUnavailableException | mitigate | GenerateDraftFromTextUseCase (Plan 03) wraps in Result.failure — no unhandled exception propagates to UI thread |
</threat_model>

<verification>
After both tasks complete:
1. All three files exist under MoneyManager/app/src/main/java/com/moneymanager/domain/ai/
2. Import scan: grep -r "import android\.\|import androidx\.\|import com\.google\." MoneyManager/app/src/main/java/com/moneymanager/domain/ai/ returns zero matches
3. gradlew :app:compileDebugKotlin exits 0
</verification>

<success_criteria>
- TransactionType.kt: 8 entries, ids match VALID_TYPES exactly, allIds() and fromId() in companion, zero Android imports
- GenAiClient.kt: single-method interface, Result return type, zero Android imports
- AiUnavailableException.kt: extends Exception, no-arg + message constructors, zero Android imports
- Kotlin compilation succeeds with all three files present
</success_criteria>

<output>
After completion, create .planning/phases/32-domain-ai-foundation/32-01-SUMMARY.md
</output>
