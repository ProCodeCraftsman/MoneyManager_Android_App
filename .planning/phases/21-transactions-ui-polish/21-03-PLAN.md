---
phase: 21-transactions-ui-polish
plan: "03"
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "No `surfaceVariant.copy(alpha = 0.5f)` (or any alpha-modified surfaceVariant) remains in TransactionsScreen.kt"
    - "All Surface backgrounds use plain MaterialTheme.colorScheme tokens (surface, surfaceContainer, surfaceContainerLow, surfaceVariant) — no .copy(alpha=) calls on color tokens used as backgrounds"
    - "LazyColumn contentPadding bottom matches the actual navigation bar height via WindowInsets.navigationBars (not a hardcoded 56.dp)"
    - "List background and bottom navigation share the same surface color — no visible color seam"
    - "App compiles and runs cleanly"
  artifacts:
    - path: "MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt"
      provides: "Single-surface theme discipline + WindowInsets.navigationBars padding"
      contains: "WindowInsets.navigationBars"
  key_links:
    - from: "LazyColumn contentPadding"
      to: "WindowInsets.navigationBars"
      via: "asPaddingValues() or .calculateBottomPadding() on the navigationBars insets"
      pattern: "WindowInsets\\.navigationBars[\\s\\S]*?asPaddingValues|calculateBottomPadding"
    - from: "all Surface/Box backgrounds"
      to: "plain MaterialTheme.colorScheme tokens"
      via: "no .copy(alpha=) on background color tokens"
      pattern: "(?!).*"
---

<objective>
Final polish per D-05 and D-06. Remove every remaining `surfaceVariant.copy(alpha = 0.5f)` (or any `.copy(alpha = ...)` on a colorScheme token used as a background) from TransactionsScreen.kt — replace with proper Material 3 tokens (`surfaceContainer`, `surfaceContainerLow`, or plain `surface`). Replace the hardcoded `PaddingValues(bottom = 56.dp)` on the LazyColumn with `WindowInsets.navigationBars.asPaddingValues()` so the list ends exactly at the system nav bar / app bottom nav, with no gap or seam.

Purpose: Single-surface discipline (D-06) and clean bottom navigation separation (D-05). This finalizes the visual redesign so all surfaces use elevation-only separation and the bottom edge has no color gap.

Output: TransactionsScreen.kt with zero alpha-modified surface tokens and contentPadding driven by WindowInsets.navigationBars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-transactions-ui-polish/21-CONTEXT.md
@.planning/phases/21-transactions-ui-polish/21-02-PLAN.md
@MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt

<interfaces>
**Pattern occurrences (from Phase 19+20 baseline) that may still exist after Plan 02:**

After Plan 02 deletes the old always-visible search Box, the remaining `surfaceVariant.copy(alpha = 0.5f)` usages are likely in transaction tile decorations (sub-row backgrounds, tag chips, etc.). Run grep to enumerate before editing.

Reusable replacement tokens:
- `MaterialTheme.colorScheme.surface` — base background, identical to Scaffold default
- `MaterialTheme.colorScheme.surfaceContainer` — slightly elevated container (use for the in-tile backgrounds)
- `MaterialTheme.colorScheme.surfaceContainerLow` — subtler than surfaceContainer
- `MaterialTheme.colorScheme.surfaceContainerHigh` — for emphasized regions

**WindowInsets API:**
```kotlin
import androidx.compose.foundation.layout.WindowInsets
import androidx.compose.foundation.layout.navigationBars
import androidx.compose.foundation.layout.asPaddingValues
```
Usage:
```kotlin
LazyColumn(
    state = lazyListState,
    contentPadding = WindowInsets.navigationBars.asPaddingValues()
)
```

If the screen has its own bottom nav within the same Composable tree (likely the parent Scaffold's bottomBar), `WindowInsets.navigationBars.asPaddingValues()` accounts for the system gesture/nav inset. The Scaffold padding lambda already supplies bottomBar height — verify by reading the parent Scaffold.

Current LazyColumn (after Plan 02):
```kotlin
LazyColumn(
    state = lazyListState,
    modifier = Modifier.fillMaxSize().padding(padding),
    contentPadding = PaddingValues(bottom = 56.dp)  // REPLACE
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove all alpha-modified surface tokens; fix bottom contentPadding</name>
  <files>MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt</files>
  <read_first>
    - MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt (full file — must enumerate all surfaceVariant.copy and PaddingValues occurrences)
    - .planning/phases/21-transactions-ui-polish/21-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    GitNexus impact check first: run `gitnexus_impact({target: "TransactionsScreen", direction: "upstream"})` and report.

    **Step A — Add WindowInsets imports** (in the alphabetical region around line 21):
    ```kotlin
    import androidx.compose.foundation.layout.WindowInsets
    import androidx.compose.foundation.layout.asPaddingValues
    import androidx.compose.foundation.layout.navigationBars
    ```
    Verify imports do not already exist before adding (check current file: `androidx.compose.foundation.layout.*` is at line 21, which means WindowInsets/navigationBars/asPaddingValues are wildcard-imported — in that case, NO new imports are required, just use them directly).

    **Step B — Enumerate alpha-on-token violations:**
    Run grep before editing:
    ```bash
    grep -n "\.copy(alpha" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
    ```
    For EACH match, classify:
    - If the alpha is applied to a `MaterialTheme.colorScheme.<token>` AND the result is used as a `color =`, `background(...)`, or Surface `color =` value → THIS IS A VIOLATION. Replace.
    - If the alpha is applied to text/icon `tint`/`color` (e.g., `onSurface.copy(alpha = 0.6f)` for secondary text) → ALLOWED, leave it. D-06 targets background discipline only.

    **Step C — Replace background-token alpha hacks:**
    For every violation found, replace `MaterialTheme.colorScheme.surfaceVariant.copy(alpha = 0.5f)` (or similar) with `MaterialTheme.colorScheme.surfaceContainer`. If the visual context is for a search-like input or pill-like background, prefer `surfaceContainerLow`. If unsure, use `surfaceContainer` (standard mid-elevation token).

    Document each replacement in the commit message body.

    **Step D — Fix LazyColumn contentPadding:**
    Locate:
    ```kotlin
    contentPadding = PaddingValues(bottom = 56.dp)
    ```
    Replace with:
    ```kotlin
    contentPadding = WindowInsets.navigationBars.asPaddingValues()
    ```

    Per D-05: "Reduce contentPadding to the actual nav bar height (likely WindowInsets.navigationBars or a fixed value matching the nav bar)". WindowInsets.navigationBars is the source of truth for the system nav bar height and adapts to gesture/3-button nav. The parent Scaffold's `padding` (which includes its bottomBar height) is already applied to LazyColumn via `Modifier.padding(padding)`, so contentPadding only needs to account for the system inset that bottomBar may not cover.

    Per D-05: list background and nav bar must use the same surface color. The LazyColumn already inherits the Scaffold's surface; verify no `.background(...)` is applied to the LazyColumn itself that would create a seam.

    **Step E — Verify no regressions:**
    After all edits:
    ```bash
    grep -c "surfaceVariant.copy(alpha" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt
    ```
    Should return 0.

    Run `gitnexus_detect_changes()` after edits.
  </action>
  <verify>
    <automated>cd MoneyManager &amp;&amp; ./gradlew compileDebugKotlin 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "surfaceVariant.copy(alpha" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 0
    - `grep -n "PaddingValues(bottom = 56" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 0 matches
    - `grep -n "WindowInsets.navigationBars.asPaddingValues" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns at least 1 match
    - `grep -n "contentPadding = WindowInsets.navigationBars" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 1 match (the LazyColumn parameter)
    - `grep -nE "\.copy\(alpha\s*=" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` — review remaining matches; each must be on a text/icon color (e.g., `onSurface.copy(alpha = 0.6f)`, `onSurfaceVariant.copy(alpha = 0.7f)`), NOT on `surface`, `surfaceVariant`, `surfaceContainer*`, or `background`. If any match has `surface*.copy(alpha`, it is a failure.
    - `grep -nE "(surface|surfaceVariant|surfaceContainer)\.copy\(alpha" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 0 matches
    - `./gradlew compileDebugKotlin` succeeds with exit code 0
    - `gitnexus_detect_changes` shows only TransactionsScreen.kt modified
  </acceptance_criteria>
  <done>
    All surface-token alpha hacks replaced with proper Material 3 tokens (surfaceContainer / surfaceContainerLow). LazyColumn contentPadding now uses WindowInsets.navigationBars.asPaddingValues. Text/icon alpha modifications preserved (D-06 targets backgrounds only). App compiles and the bottom edge of the list aligns cleanly with the bottom navigation, no gap.
  </done>
</task>

</tasks>

<verification>
- App compiles: `cd MoneyManager && ./gradlew assembleDebug` succeeds
- `grep -c "surfaceVariant.copy(alpha" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 0
- `grep "WindowInsets.navigationBars" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns at least 1 match
- `grep "PaddingValues(bottom = 56" MoneyManager/app/src/main/java/com/moneymanager/app/ui/screens/TransactionsScreen.kt` returns 0 matches
- Visual: scrolling to the bottom of the transaction list shows no color seam between the last item and the bottom navigation
- `gitnexus_detect_changes()` confirms only TransactionsScreen.kt changed
</verification>

<success_criteria>
- Single-surface discipline: zero alpha-modified surface tokens in the entire file
- All Surface/Box backgrounds use plain MaterialTheme.colorScheme.* tokens
- LazyColumn contentPadding tied to WindowInsets.navigationBars (responsive to device chrome)
- No visible gap between transaction list and bottom navigation
- Phase 21 redesign complete: layout matches CONTEXT.md ASCII targets across at-rest, scrolled, and search-active states
</success_criteria>

<output>
After completion, create `.planning/phases/21-transactions-ui-polish/21-03-SUMMARY.md`
</output>
