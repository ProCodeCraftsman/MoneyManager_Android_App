# Quick Task: fix the gaps found in the export templates

**Task ID:** 20260430-001
**Date:** 2026-04-30
**Status:** Planning Complete

## Overview

Fix gaps in ExportRepository.kt where TransactionEntity fields are not properly exported/imported in JSON and CSV formats.

## Gaps Identified

### Missing from JSON Export/Import:
- `subCategoryId`, `goalId`, `peerContactId`, `description`, `receiptPath`
- `recurringId`, `splitData`, `investmentPlatform`, `expectedReturnDate`, `createdAt`
- `isSplitParent`, `isSplitChild`, `parentTransactionId`

### Missing from CSV Export/Import:
- `tagIds`, `subCategoryId`, `goalId`, `peerContactId`, `description`, `receiptPath`
- `recurringId`, `splitData`, `investmentPlatform`, `expectedReturnDate`, `createdAt`

## Tasks

### Task 1: Update JSON Export for Transactions
**Files:** `MoneyManager/app/src/main/java/com/moneymanager/data/repository/ExportRepository.kt`

**Action:**
- Add missing fields to `exportTransactions()` JSON export function: subCategoryId, goalId, peerContactId, description, receiptPath, recurringId, splitData, investmentPlatform, expectedReturnDate, createdAt, isSplitParent, isSplitChild, parentTransactionId

**Verify:**
- All 23 transaction fields exported in JSON (excluding id, accountId, type, amount, categoryId, date, note, isRecurring which already exist)

**Done:** JSON export includes all TransactionEntity fields

---

### Task 2: Update JSON Import for Transactions
**Files:** `MoneyManager/app/src/main/java/com/moneymanager/data/repository/ExportRepository.kt`

**Action:**
- Add missing fields to `importTransactions()` JSON import function to read and map: subCategoryId, goalId, peerContactId, description, receiptPath, recurringId, splitData, investmentPlatform, expectedReturnDate, createdAt, isSplitParent, isSplitChild, parentTransactionId

**Verify:**
- ImportResult includes all imported transaction fields properly mapped

**Done:** JSON import reads all TransactionEntity fields

---

### Task 3: Update CSV Export for Transactions
**Files:** `MoneyManager/app/src/main/java/com/moneymanager/data/repository/ExportRepository.kt`

**Action:**
- Update `exportTransactionsCsv()` to include: tagIds, subCategoryId, goalId, peerContactId, description, receiptPath, recurringId, splitData, investmentPlatform, expectedReturnDate, createdAt
- Update CSV header to include new fields

**Verify:**
- CSV export contains all transaction fields (except isSplitParent, isSplitChild, parentTransactionId, toAccountId, isTransfer which are already handled)

**Done:** CSV export includes all missing TransactionEntity fields

---

### Task 4: Update CSV Import for Transactions
**Files:** `MoneyManager/app/src/main/java/com/moneymanager/data/repository/ExportRepository.kt`

**Action:**
- Update `importTransactionsFromCsv()` to parse and import: tagIds, subCategoryId, goalId, peerContactId, description, receiptPath, recurringId, splitData, investmentPlatform, expectedReturnDate, createdAt
- Update field parsing to handle the expanded CSV format

**Verify:**
- CSV import properly maps all transaction fields to TransactionEntity

**Done:** CSV import reads all TransactionEntity fields

---

## Summary

4 focused tasks to update ExportRepository.kt to handle all TransactionEntity fields in both JSON and CSV export/import formats.
